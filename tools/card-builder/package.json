{
  "name": "ptcg-card-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Local-only UI for scaffolding PTCG cards from existing prefabs",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
