// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, SHOW_CARDS_TO_PLAYER, SHUFFLE_DECK, NEXT_TURN_ATTACK_BONUS } from '../../game/store/prefabs/prefabs';

export class Ludicolo2 extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Lombre';
  public cardType: CardType = W;
  public hp: number = 130;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public readonly SPLASH_DANCE_MARKER = 'LUDICOLO2_SPLASH_DANCE_MARKER';
  public readonly SPLASH_DANCE_MARKER_2 = 'LUDICOLO2_SPLASH_DANCE_MARKER_2';

  public attacks = [
    {
      name: 'Astonish',
      cost: [C],
      damage: 30,
      text: 'Choose a random card from your opponent\'s hand. Your opponent reveals that card and shuffles it into his or her deck.'
    },
    {
      name: 'Splash Dance',
      cost: [W, C, C],
      damage: 60,
      text: 'During your next turn, this Pokémon\'s Splash Dance attack does 60 more damage (before applying Weakness and Resistance).'
    }
  ];

  public set: string = 'PRC';
  public setNumber: string = '37';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ludicolo';
  public fullName: string = 'Ludicolo PRC 37';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Astonish
    // Ref: set-plasma-storm/foongus.ts (Astonish)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (opponent.hand.cards.length > 0) {
        const randomIndex = Math.floor(Math.random() * opponent.hand.cards.length);
        const randomCard = opponent.hand.cards[randomIndex];
        SHOW_CARDS_TO_PLAYER(store, state, player, [randomCard]);
        opponent.hand.moveCardTo(randomCard, opponent.deck);
        SHUFFLE_DECK(store, state, opponent);
      }
    }

    // Attack 2: Splash Dance
    // Ref: set-phantom-forces/gigalith.ts (Overdrive Smash)
    NEXT_TURN_ATTACK_BONUS(effect, {
      attack: this.attacks[1],
      source: this,
      bonusDamage: 60,
      bonusMarker: this.SPLASH_DANCE_MARKER,
      clearMarker: this.SPLASH_DANCE_MARKER_2
    });

    return state;
  }
}
