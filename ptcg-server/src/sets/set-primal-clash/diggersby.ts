// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StateUtils, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, FLIP_UNTIL_TAILS_AND_COUNT_HEADS, MOVE_CARDS } from '../../game/store/prefabs/prefabs';

export class Diggersby extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Bunnelby';
  public cardType: CardType = F;
  public hp: number = 110;
  public weakness = [{ type: G }];
  public retreat = [C, C, C];

  public attacks = [
    {
      name: 'Ear Dig',
      cost: [F],
      damage: 0,
      text: 'Flip a coin until you get tails. For each heads, discard the top card of your opponent\'s deck.'
    },
    {
      name: 'Hammer In',
      cost: [C, C, C],
      damage: 70,
      text: ''
    }
  ];

  public set: string = 'PRC';
  public setNumber: string = '90';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Diggersby';
  public fullName: string = 'Diggersby PRC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Ear Dig
    // Ref: AGENTS-patterns.md (flip until tails), set-primal-clash/bunnelby.ts (Burrow - deck discard)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      return FLIP_UNTIL_TAILS_AND_COUNT_HEADS(store, state, player, heads => {
        if (heads > 0) {
          const cardsToDiscard = Math.min(heads, opponent.deck.cards.length);
          if (cardsToDiscard > 0) {
            MOVE_CARDS(store, state, opponent.deck, opponent.discard, { count: cardsToDiscard, sourceCard: this, sourceEffect: effect });
          }
        }
      });
    }

    return state;
  }
}
