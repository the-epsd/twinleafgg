// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SuperType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { CheckPokemonTypeEffect } from '../../game/store/effects/check-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND } from '../../game/store/prefabs/prefabs';

export class Magnezone extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Magneton';
  public cardType: CardType = L;
  public hp: number = 160;
  public weakness = [{ type: F }];
  public resistance = [{ type: M, value: -20 }];
  public retreat = [C, C, C];

  public powers = [  {
    name: 'Double Type',
    powerType: PowerType.ABILITY,
    text: 'As long as this Pokémon is in play, it is Lightning and Metal type.'
  }];

  public attacks = [
    {
      name: 'Magnetic Bolt',
      cost: [L, C, C],
      damage: 120,
      text: 'Put a Trainer card from your discard pile into your hand.'
    }
  ];

  public set: string = 'UNM';
  public setNumber: string = '60';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Magnezone';
  public fullName: string = 'Magnezone UNM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Double Type (passive - add Metal type)
    // Ref: set-team-up/water-memory.ts (CheckPokemonTypeEffect pattern)
    if (effect instanceof CheckPokemonTypeEffect && effect.target.cards.includes(this) && effect.target.getPokemonCard() === this) {
      const player = StateUtils.findOwner(state, effect.target);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      if (!effect.cardTypes.includes(CardType.METAL)) {
        effect.cardTypes.push(CardType.METAL);
      }
    }

    // Attack 1: Magnetic Bolt
    // Ref: set-team-up/galvantula.ts (SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND(store, state, player, this,
        { superType: SuperType.TRAINER },
        { min: 1, max: 1, allowCancel: false }
      );
    }

    return state;
  }
}
