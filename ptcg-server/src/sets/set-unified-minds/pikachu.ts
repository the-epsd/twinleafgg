// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, FLIP_UNTIL_TAILS_AND_COUNT_HEADS, DRAW_CARDS } from '../../game/store/prefabs/prefabs';

export class Pikachu extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = L;
  public hp: number = 60;
  public weakness = [{ type: F }];
  public resistance = [{ type: M, value: -20 }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Meal Time',
      cost: [L],
      damage: 0,
      text: 'Flip a coin until you get tails. For each heads, draw a card.'
    },
    {
      name: 'Gnaw',
      cost: [L, C],
      damage: 20,
      text: ''
    }
  ];

  public set: string = 'UNM';
  public setNumber: string = '55';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Pikachu';
  public fullName: string = 'Pikachu UNM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Meal Time
    // Ref: set-lost-thunder/pineco.ts (flip until tails, draw per heads)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      FLIP_UNTIL_TAILS_AND_COUNT_HEADS(store, state, player, heads => {
        if (heads > 0) {
          DRAW_CARDS(player, heads);
        }
      });
    }

    return state;
  }
}
