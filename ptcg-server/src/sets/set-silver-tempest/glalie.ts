// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { TrainerEffect } from '../../game/store/effects/play-card-effects';

export class Glalie extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Snorunt';
  public cardType: CardType = W;
  public hp: number = 130;
  public weakness = [{ type: M }];
  public retreat = [C, C];

  public readonly CANDICE_MARKER = 'GLALIE_SIT_CANDICE_MARKER';

  public attacks = [
    {
      name: 'Bite',
      cost: [W, C, C],
      damage: 60,
      text: ''
    },
    {
      name: 'Big Mouth',
      cost: [C, C, C, C],
      damage: 130,
      damageCalculation: '+' as '+',
      text: 'If you played Candice from your hand during this turn, this attack does 130 more damage.'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'SIT';
  public setNumber: string = '42';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Glalie';
  public fullName: string = 'Glalie SIT 42';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Track when Candice was played this turn
    // Ref: set-chilling-reign/zangoose.ts (track specific supporter by name)
    if (effect instanceof TrainerEffect && effect.trainerCard.name === 'Candice') {
      ADD_MARKER(this.CANDICE_MARKER, effect.player, this);
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.CANDICE_MARKER, this);

    // Attack 2: Big Mouth
    // Ref: set-brilliant-stars/morpeko-v.ts (conditional bonus damage based on supporter marker)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      if (HAS_MARKER(this.CANDICE_MARKER, player, this)) {
        effect.damage += 130;
      }
    }

    return state;
  }
}
