// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Keldeo extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = W;
  public hp: number = 120;
  public weakness = [{ type: L }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Wave Splash',
      cost: [W],
      damage: 20,
      text: ''
    },
    {
      name: 'Four as One',
      cost: [W, C, C],
      damage: 50,
      damageCalculation: '+' as '+',
      text: 'If Cobalion, Terrakion, and Virizion are on your Bench, this attack does 170 more damage.'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'SIT';
  public setNumber: string = '46';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Keldeo';
  public fullName: string = 'Keldeo (SIT 46)';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 2: Four as One
    // Ref: set-pokemon-151/nidoking.ts (bench Pokemon name check)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;

      const hasCobalion = player.bench.some(b => b.getPokemonCard()?.name === 'Cobalion');
      const hasTerrakion = player.bench.some(b => b.getPokemonCard()?.name === 'Terrakion');
      const hasVirizion = player.bench.some(b => b.getPokemonCard()?.name === 'Virizion');

      if (hasCobalion && hasTerrakion && hasVirizion) {
        effect.damage += 170;
      }
    }

    return state;
  }
}
