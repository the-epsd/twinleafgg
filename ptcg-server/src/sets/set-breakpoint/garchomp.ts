// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag } from '../../game/store/card/card-types';
import { StoreLike, State, PlayerType, SlotType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ATTACH_ENERGY_PROMPT, DEAL_MORE_DAMAGE_IF_OPPONENT_ACTIVE_HAS_CARD_TAG } from '../../game/store/prefabs/prefabs';

export class Garchomp extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Gabite';
  public cardType: CardType = F;
  public hp: number = 130;
  public weakness = [{ type: G }];
  public retreat = [];

  public attacks = [
    {
      name: 'Turbo Assault',
      cost: [F],
      damage: 60,
      text: 'Attach an Energy card from your discard pile to 1 of your Pokémon.'
    },
    {
      name: 'Bite Off',
      cost: [F, F],
      damage: 80,
      damageCalculation: '+' as '+',
      text: 'If your opponent\'s Active Pokémon is a Pokémon-EX, this attack does 80 more damage.'
    }
  ];

  public set: string = 'BKP';
  public setNumber: string = '70';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Garchomp';
  public fullName: string = 'Garchomp BKP';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Turbo Assault
    // Ref: AGENTS-patterns.md (Attach energy from discard)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      ATTACH_ENERGY_PROMPT(store, state, player, PlayerType.BOTTOM_PLAYER, SlotType.DISCARD,
        [SlotType.ACTIVE, SlotType.BENCH], {}, { min: 0, max: 1 });
    }

    // Attack 2: Bite Off
    // Ref: set-fates-collide/lugia.ts (Intensifying Burn)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      DEAL_MORE_DAMAGE_IF_OPPONENT_ACTIVE_HAS_CARD_TAG(effect, state, 80, CardTag.POKEMON_EX);
    }

    return state;
  }
}
