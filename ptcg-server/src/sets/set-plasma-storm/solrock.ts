// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { ADD_BURN_TO_PLAYER_ACTIVE, AFTER_ATTACK, THIS_POKEMON_DOES_DAMAGE_TO_ITSELF, WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';
import { CardType, Stage } from '../../game/store/card/card-types';
import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Effect } from '../../game/store/effects/effect';
import { State, StoreLike } from '../../game';
export class Solrock extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = F;
  public hp: number = 90;
  public weakness = [{ type: G }];
  public retreat = [C, C, C];

  public attacks = [
    {
      name: 'Heat Burn',
      cost: [F, C],
      damage: 20,
      text: 'The Defending Pokémon is now Burned.'
    },
    {
      name: 'Explosion',
      cost: [F, C, C],
      damage: 90,
      text: 'This Pokémon does 90 damage to itself.'
    }
  ];

  public set: string = 'PLS';
  public setNumber: string = '74';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Solrock';
  public fullName: string = 'Solrock PLS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Heat Burn
    // Ref: set-boundaries-crossed/camerupt.ts (Searing Flame)
    if (AFTER_ATTACK(effect, 0, this)) {
      ADD_BURN_TO_PLAYER_ACTIVE(store, state, effect.opponent, this);
    }

    // Attack 2: Explosion
    // Ref: set-boundaries-crossed/darumaka.ts (self-damage pattern)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      THIS_POKEMON_DOES_DAMAGE_TO_ITSELF(store, state, effect, 90);
    }

    return state;
  }
}
