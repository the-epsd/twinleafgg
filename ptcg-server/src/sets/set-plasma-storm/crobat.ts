// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, WAS_POWER_USED, DRAW_CARDS, ABILITY_USED, USE_ABILITY_ONCE_PER_TURN, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_POISIONED } from '../../game/store/prefabs/attack-effects';

export class Crobat extends PokemonCard {
  public tags = [CardTag.TEAM_PLASMA];
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Golbat';
  public cardType: CardType = P;
  public hp: number = 130;
  public weakness = [{ type: L }];
  public resistance = [{ type: F, value: -20 }];
  public retreat = [];

  public powers = [  {
    name: 'Night Sight',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may draw a card.'
  }];

  public attacks = [
    {
      name: 'Ultra-Toxic Fang',
      cost: [P, C, C],
      damage: 40,
      text: 'The Defending Pokémon is now Poisoned. Put 4 damage counters instead of 1 on this Pokémon between turns.'
    }
  ];

  public set: string = 'PLS';
  public setNumber: string = '55';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Crobat';
  public fullName: string = 'Crobat PLS';
  public readonly NIGHT_SIGHT_MARKER = 'NIGHT_SIGHT_MARKER';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Night Sight
    // Ref: set-boundaries-crossed/audino.ts (once per turn draw ability)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;
      USE_ABILITY_ONCE_PER_TURN(player, this.NIGHT_SIGHT_MARKER, this);
      ABILITY_USED(player, this);
      DRAW_CARDS(player, 1);
    }

    // Attack 1: Ultra-Toxic Fang
    // Ref: AGENTS-patterns.md (custom poison damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const opponent = StateUtils.getOpponent(state, effect.player);
      YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_POISIONED(store, state, effect);
      opponent.active.poisonDamage = 40;
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.NIGHT_SIGHT_MARKER, this);

    return state;
  }
}
