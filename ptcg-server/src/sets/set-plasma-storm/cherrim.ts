// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, PlayerType, SlotType, GameError, GameMessage, StoreLike, State, CardTarget } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { HealEffect } from '../../game/store/effects/game-effects';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, MULTIPLE_COIN_FLIPS_PROMPT, USE_ABILITY_ONCE_PER_TURN, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { ChoosePokemonPrompt } from '../../game/store/prompts/choose-pokemon-prompt';
import { StateUtils } from '../../game/store/state-utils';

export class Cherrim extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Cherubi';
  public cardType: CardType = G;
  public hp: number = 70;
  public weakness = [{ type: R }];
  public resistance = [{ type: W, value: -20 }];
  public retreat = [C];

  public powers = [  {
    name: 'Fair-Weather Heal',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may heal 20 damage from 1 of your Pokémon that has any [G] Energy attached to it.'
  }];

  public attacks = [
    {
      name: 'Random Peck',
      cost: [G, C],
      damage: 20,
      damageCalculation: '+' as const,
      text: 'Flip 2 coins. This attack does 20 more damage for each heads.'
    }
  ];

  public set: string = 'PLS';
  public setNumber: string = '7';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Cherrim';
  public fullName: string = 'Cherrim PLS';

  public readonly FAIR_WEATHER_HEAL_MARKER = 'FAIR_WEATHER_HEAL_MARKER';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Fair-Weather Heal
    // Ref: set-ultra-prism/gardenia.ts (heal Pokemon with Grass Energy)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      // Find Pokemon with Grass energy and damage
      const blocked: CardTarget[] = [];
      let hasTarget = false;

      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
        const checkEnergy = new CheckProvidedEnergyEffect(player, cardList);
        store.reduceEffect(state, checkEnergy);
        const hasGrassEnergy = StateUtils.checkEnoughEnergy(checkEnergy.energyMap, [CardType.GRASS]);

        if (cardList.damage === 0 || !hasGrassEnergy) {
          blocked.push(target);
        } else {
          hasTarget = true;
        }
      });

      if (!hasTarget) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.FAIR_WEATHER_HEAL_MARKER, this);

      store.prompt(state, new ChoosePokemonPrompt(
        player.id,
        GameMessage.CHOOSE_POKEMON_TO_HEAL,
        PlayerType.BOTTOM_PLAYER,
        [SlotType.ACTIVE, SlotType.BENCH],
        { allowCancel: false, blocked }
      ), targets => {
        if (targets && targets.length > 0) {
          const healEffect = new HealEffect(player, targets[0], 20);
          store.reduceEffect(state, healEffect);
        }
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.FAIR_WEATHER_HEAL_MARKER, this);

    // Attack 1: Random Peck
    // Ref: set-plasma-storm/piloswine.ts (multiple coin flips for damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      MULTIPLE_COIN_FLIPS_PROMPT(store, state, effect.player, 2, results => {
        const heads = results.filter(r => r).length;
        effect.damage += 20 * heads;
      });
    }

    return state;
  }
}
