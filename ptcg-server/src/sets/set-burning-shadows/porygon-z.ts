// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, ConfirmPrompt, GameMessage, PlayerType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, JUST_EVOLVED, IS_ABILITY_BLOCKED, DEVOLVE_POKEMON } from '../../game/store/prefabs/prefabs';

export class PorygonZ extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Porygon2';
  public cardType: CardType = C;
  public hp: number = 130;
  public weakness = [{ type: F }];
  public retreat = [C, C];

  public powers = [{
    name: 'Initialize',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand to evolve 1 of your Pokémon during your turn, you may devolve each of your opponent\'s evolved Pokémon by putting the highest Stage Evolution card on it into your opponent\'s hand.'
  }];

  public attacks = [
    {
      name: 'Zap Cannon',
      cost: [C, C, C],
      damage: 80,
      text: 'This Pokémon can\'t use Zap Cannon during your next turn.'
    }
  ];

  public set: string = 'BUS';
  public setNumber: string = '105';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Porygon-Z';
  public fullName: string = 'Porygon-Z BUS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Initialize (on-evolve)
    // Ref: set-guardians-rising/salazzle.ts (Hot Poison - on-evolve ability)
    if (JUST_EVOLVED(effect, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const opponent = StateUtils.getOpponent(state, player);

      state = store.prompt(state, new ConfirmPrompt(
        player.id,
        GameMessage.WANT_TO_USE_ABILITY
      ), wantToUse => {
        if (wantToUse) {
          // Devolve each of opponent's evolved Pokemon
          opponent.forEachPokemon(PlayerType.TOP_PLAYER, (cardList) => {
            DEVOLVE_POKEMON(store, state, cardList, opponent.hand);
          });
        }
      });
    }

    // Attack 1: Zap Cannon
    // Ref: AGENTS.md (self-attack restriction)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      effect.player.active.cannotUseAttacksNextTurnPending.push('Zap Cannon');
    }

    return state;
  }
}
