// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, PlayerType } from '../../game';
import { HealTargetEffect } from '../../game/store/effects/attack-effects';
import { HealEffect } from '../../game/store/effects/game-effects';
import { Effect } from '../../game/store/effects/effect';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Lunatone extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = F;
  public hp: number = 90;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public powers = [{
    name: 'Heal Block',
    powerType: PowerType.ABILITY,
    text: 'If you have Solrock in play, Pokémon (both yours and your opponent\'s) can\'t be healed.'
  }];

  public attacks = [
    {
      name: 'Lunar Blast',
      cost: [F, C],
      damage: 30,
      text: ''
    }
  ];

  public set: string = 'BUS';
  public setNumber: string = '68';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Lunatone';
  public fullName: string = 'Lunatone BUS';

  private hasSolrockInPlay(state: State, player: any): boolean {
    let found = false;
    player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList: any, card: PokemonCard) => {
      if (card.name === 'Solrock') {
        found = true;
      }
    });
    return found;
  }

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Heal Block (passive)
    // Ref: set-sword-and-shield/toxicroak.ts (More Poison - passive ability pattern)
    if (effect instanceof HealTargetEffect || effect instanceof HealEffect) {
      // Find Lunatone's owner
      let lunatoneOwner: any = null;
      state.players.forEach(player => {
        player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card) => {
          if (card === this) {
            lunatoneOwner = player;
          }
        });
      });

      if (!lunatoneOwner) {
        return state;
      }

      if (IS_ABILITY_BLOCKED(store, state, lunatoneOwner, this)) {
        return state;
      }

      if (!this.hasSolrockInPlay(state, lunatoneOwner)) {
        return state;
      }

      // Block the healing
      effect.preventDefault = true;
    }

    return state;
  }
}
