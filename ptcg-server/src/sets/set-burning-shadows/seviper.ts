// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { ADD_POISON_TO_PLAYER_ACTIVE, AFTER_ATTACK } from '../../game/store/prefabs/prefabs';
import { CardType, SpecialCondition, Stage } from '../../game/store/card/card-types';
import { PowerEffect } from '../../game/store/effects/game-effects';
import { StateUtils } from '../../game/store/state-utils';
import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Effect } from '../../game/store/effects/effect';
import { PlayerType, PowerType, State, StoreLike } from '../../game';
import { BetweenTurnsEffect } from '../../game/store/effects/game-phase-effects';
export class Seviper extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = P;
  public hp: number = 100;
  public weakness = [{ type: P }];
  public retreat = [C, C];

  public powers = [{
    name: 'More Poison',
    powerType: PowerType.ABILITY,
    text: 'Put 1 more damage counter on your opponent\'s Poisoned Pokémon between turns.'
  }];

  public attacks = [
    {
      name: 'Venomous Fang',
      cost: [P, C, C],
      damage: 30,
      text: 'Your opponent\'s Active Pokémon is now Poisoned.'
    }
  ];

  public set: string = 'BUS';
  public setNumber: string = '50';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Seviper';
  public fullName: string = 'Seviper BUS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: More Poison (passive)
    // Ref: set-sword-and-shield/toxicroak.ts (More Poison)
    if (effect instanceof BetweenTurnsEffect) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      let seviperOwner: any = null;
      [player, opponent].forEach(p => {
        p.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card) => {
          if (card === this) {
            seviperOwner = p;
          }
        });
      });

      if (!seviperOwner) {
        return state;
      }

      try {
        const stub = new PowerEffect(seviperOwner, {
          name: 'test',
          powerType: PowerType.ABILITY,
          text: ''
        }, this);
        store.reduceEffect(state, stub);
      } catch {
        return state;
      }

      const seviperOpponent = StateUtils.getOpponent(state, seviperOwner);
      if (effect.player === seviperOpponent && seviperOpponent.active.specialConditions.includes(SpecialCondition.POISONED)) {
        effect.poisonDamage += 10;
      }
    }

    // Attack 1: Venomous Fang
    // Ref: set-guardians-rising/helioptile.ts (special condition pattern)
    if (AFTER_ATTACK(effect, 0, this)) {
      ADD_POISON_TO_PLAYER_ACTIVE(store, state, effect.opponent, this);
    }

    return state;
  }
}
