// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PutDamageEffect } from '../../game/store/effects/attack-effects';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Gyarados extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Magikarp';
  public cardType: CardType = W;
  public hp: number = 150;
  public weakness = [{ type: L }];
  public retreat = [C, C, C, C];

  public attacks = [
    {
      name: 'Venting Anger',
      cost: [C, C],
      damage: 50,
      damageCalculation: 'x' as 'x',
      text: 'This attack does 50 damage for each Magikarp in your discard pile.'
    },
    {
      name: 'Splash Burn',
      cost: [W, W, C, C],
      damage: 160,
      text: 'This attack does 30 damage to each of your Benched Pokémon. (Don\'t apply Weakness and Resistance for Benched Pokémon.)'
    }
  ];

  public set: string = 'BUS';
  public setNumber: string = '33';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Gyarados';
  public fullName: string = 'Gyarados BUS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Venting Anger
    // Ref: set-guardians-rising/gyarados.ts (similar Magikarp count pattern)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;

      let magikarpCount = 0;
      player.discard.cards.forEach(c => {
        if (c instanceof PokemonCard && c.name === 'Magikarp') {
          magikarpCount += 1;
        }
      });

      effect.damage = 50 * magikarpCount;
    }

    // Attack 2: Splash Burn
    // Ref: set-guardians-rising/turtonator.ts (Bright Flame - self bench damage)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;

      player.bench.forEach(benched => {
        if (benched.cards.length > 0) {
          const damage = new PutDamageEffect(effect, 30);
          damage.target = benched;
          store.reduceEffect(state, damage);
        }
      });
    }

    return state;
  }
}
