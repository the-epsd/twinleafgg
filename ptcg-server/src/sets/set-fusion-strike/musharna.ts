// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED, GUST_OPPONENT_BENCHED_POKEMON } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP } from '../../game/store/prefabs/attack-effects';

export class Musharna extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Munna';
  public cardType: CardType = P;
  public hp: number = 120;
  public weakness = [{ type: D }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Sleep Inducer',
      cost: [C],
      damage: 0,
      text: 'Switch 1 of your opponent\'s Benched Pokémon with their Active Pokémon. The new Active Pokémon is now Asleep.'
    },
    {
      name: 'Psychic',
      cost: [P, C],
      damage: 30,
      damageCalculation: '+' as '+',
      text: 'This attack does 30 more damage for each Energy attached to your opponent\'s Active Pokémon.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'FST';
  public setNumber: string = '122';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Musharna';
  public fullName: string = 'Musharna FST 122';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Sleep Inducer - gust opponent's benched Pokemon, then it's Asleep
    // Ref: set-unified-minds/araquanid.ts (GUST_OPPONENT_BENCHED_POKEMON with onSwitched callback)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;

      state = GUST_OPPONENT_BENCHED_POKEMON(store, state, player, {
        onSwitched: () => {
          YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP(store, state, effect);
        }
      });

      return state;
    }

    // Attack 2: Psychic - +30 per energy on opponent's active
    // Ref: set-sun-and-moon/hypno.ts (+30 per energy on opponent's active with CheckProvidedEnergyEffect)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      const checkEnergy = new CheckProvidedEnergyEffect(player, opponent.active);
      store.reduceEffect(state, checkEnergy);
      let energyCount = 0;
      checkEnergy.energyMap.forEach(em => {
        energyCount += em.provides.length;
      });
      effect.damage += 30 * energyCount;
    }

    return state;
  }
}
