// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, SlotType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, ATTACH_X_TYPE_ENERGY_FROM_DISCARD_TO_1_OF_YOUR_POKEMON } from '../../game/store/prefabs/prefabs';

export class Grubbin extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = G;
  public hp: number = 70;
  public weakness = [{ type: R }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Energize',
      cost: [C],
      damage: 0,
      text: 'Attach a [L] Energy card from your discard pile to this Pokémon.'
    },
    {
      name: 'Surprise Attack',
      cost: [C, C, C],
      damage: 50,
      text: 'Flip a coin. If tails, this attack does nothing.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'FST';
  public setNumber: string = '18';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Grubbin';
  public fullName: string = 'Grubbin (FST 18)';
  public legacyFullName = 'Grubbin FST';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Energize
    // Ref: set-noble-victories/eelektrik.ts (attach L energy from discard, active only)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      ATTACH_X_TYPE_ENERGY_FROM_DISCARD_TO_1_OF_YOUR_POKEMON(
        store, state, player, 1, CardType.LIGHTNING,
        { destinationSlots: [SlotType.ACTIVE], min: 1, allowCancel: false }
      );
    }

    // Attack 2: Surprise Attack
    // Ref: AGENTS-patterns.md (Coin Flip - if tails this attack does nothing)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      COIN_FLIP_PROMPT(store, state, player, result => {
        if (!result) {
          effect.damage = 0;
        }
      });
    }

    return state;
  }
}
