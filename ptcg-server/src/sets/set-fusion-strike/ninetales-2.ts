// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, PlayerType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckRetreatCostEffect, CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Ninetales2 extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Vulpix';
  public cardType: CardType = R;
  public hp: number = 120;
  public weakness = [{ type: W }];
  public retreat = [C, C];

  public powers = [{
    name: 'Byway of the Nine-Tailed Fox',
    powerType: PowerType.ABILITY,
    text: 'The Retreat Cost of each of your Pokémon that has any [R] Energy attached is ColorlessColorless less.'
  }];

  public attacks = [
    {
      name: 'Flame Tail',
      cost: [R, C],
      damage: 60,
      text: ''
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'FST';
  public setNumber: string = '31';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ninetales';
  public fullName: string = 'Ninetales FST 31';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Byway of the Nine-Tailed Fox (passive)
    // Ref: set-surging-sparks/latias-ex.ts (Skyliner - CheckRetreatCostEffect for retreat cost reduction)
    if (effect instanceof CheckRetreatCostEffect) {
      const player = effect.player;

      // Check if this Ninetales is in play for this player
      const cardList = StateUtils.findCardList(state, this);
      const owner = StateUtils.findOwner(state, cardList);

      if (owner !== player) {
        return state;
      }

      let isNinetalesInPlay = false;
      owner.forEachPokemon(PlayerType.BOTTOM_PLAYER, (list, card) => {
        if (card === this) {
          isNinetalesInPlay = true;
        }
      });

      if (!isNinetalesInPlay) {
        return state;
      }

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      // Check if the retreating Pokemon has any Fire energy attached
      const checkEnergy = new CheckProvidedEnergyEffect(player, player.active);
      store.reduceEffect(state, checkEnergy);

      const hasFireEnergy = checkEnergy.energyMap.some(em =>
        em.provides.some((t: CardType) => t === CardType.FIRE || t === CardType.ANY)
      );

      if (hasFireEnergy) {
        // Reduce retreat cost by 2 Colorless
        let removed = 0;
        effect.cost = effect.cost.filter((c: CardType) => {
          if (removed < 2 && c === CardType.COLORLESS) {
            removed++;
            return false;
          }
          return true;
        });
      }
    }

    return state;
  }
}
