// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, MULTIPLE_COIN_FLIPS_PROMPT } from '../../game/store/prefabs/prefabs';

export class Corvisquire extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Rookidee';
  public cardType: CardType = C;
  public hp: number = 80;
  public weakness = [{ type: L }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Fury Attack',
      cost: [C, C],
      damage: 30,
      damageCalculation: 'x' as 'x',
      text: 'Flip 3 coins. This attack does 30 damage for each heads.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'FST';
  public setNumber: string = '220';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Corvisquire';
  public fullName: string = 'Corvisquire (FST 220)';
  public legacyFullName = 'Corvisquire FST';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Fury Attack
    // Ref: set-darkness-ablaze/corvisquire.ts (Fury Attack)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      MULTIPLE_COIN_FLIPS_PROMPT(store, state, player, 3, results => {
        const heads = results.filter(r => r).length;
        effect.damage = 30 * heads;
      });
    }

    return state;
  }
}
