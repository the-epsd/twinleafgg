// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, MULTIPLE_COIN_FLIPS_PROMPT } from '../../game/store/prefabs/prefabs';
import { DISCARD_X_ENERGY_FROM_THIS_POKEMON } from '../../game/store/prefabs/costs';

export class Magcargo extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Slugma';
  public cardType: CardType = R;
  public hp: number = 130;
  public weakness = [{ type: W }];
  public retreat = [C, C, C];

  public attacks = [
    {
      name: 'Rock Throw',
      cost: [C, C],
      damage: 40,
      text: ''
    },
    {
      name: 'Body Splash',
      cost: [R, R, C],
      damage: 150,
      text: 'Flip 3 coins. For each tails, discard an Energy from this Pokémon.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'FST';
  public setNumber: string = '35';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Magcargo';
  public fullName: string = 'Magcargo FST 35';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 2: Body Splash
    // Ref: set-fusion-strike/delcatty.ts (MULTIPLE_COIN_FLIPS_PROMPT), set-x-and-y/slugma.ts (DISCARD_X_ENERGY_FROM_THIS_POKEMON)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      MULTIPLE_COIN_FLIPS_PROMPT(store, state, effect.player, 3, results => {
        const tails = results.filter(r => !r).length;
        if (tails > 0) {
          DISCARD_X_ENERGY_FROM_THIS_POKEMON(store, state, effect, tails);
        }
      });
    }

    return state;
  }
}
