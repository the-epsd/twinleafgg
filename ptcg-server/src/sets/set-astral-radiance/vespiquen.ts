// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, GameMessage } from '../../game';
import { TrainerCard } from '../../game/store/card/trainer-card';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';
import { ChooseCardsPrompt } from '../../game/store/prompts/choose-cards-prompt';
import { ShowCardsPrompt } from '../../game/store/prompts/show-cards-prompt';

export class Vespiquen extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Combee';
  public cardType: CardType = G;
  public hp: number = 120;
  public weakness = [{ type: R }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Honey Rush',
      cost: [G],
      damage: 60,
      damageCalculation: 'x' as 'x',
      text: 'Reveal any number of Sweet Honey cards from your hand. This attack does 60 damage for each card you revealed in this way.'
    },
    {
      name: 'Pierce',
      cost: [G, C, C],
      damage: 90,
      text: ''
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'ASR';
  public setNumber: string = '12';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Vespiquen';
  public fullName: string = 'Vespiquen (ASR 12)';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Honey Rush
    // Ref: set-lost-thunder/miltank.ts (Milk Cannon - reveal named cards from hand for damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = effect.opponent;

      const sweetHoneyCards = player.hand.cards.filter(c =>
        c instanceof TrainerCard && c.name === 'Sweet Honey'
      );

      if (sweetHoneyCards.length === 0) {
        effect.damage = 0;
        return state;
      }

      const blocked: number[] = [];
      player.hand.cards.forEach((c, index) => {
        if (!(c instanceof TrainerCard && c.name === 'Sweet Honey')) {
          blocked.push(index);
        }
      });

      store.prompt(state, new ChooseCardsPrompt(
        player,
        GameMessage.CHOOSE_CARD_TO_HAND,
        player.hand,
        {},
        { min: 0, max: sweetHoneyCards.length, allowCancel: false, blocked }
      ), selected => {
        const cards = selected || [];
        effect.damage = 60 * cards.length;

        if (cards.length > 0) {
          store.prompt(state, new ShowCardsPrompt(
            opponent.id,
            GameMessage.CARDS_SHOWED_BY_THE_OPPONENT,
            cards
          ), () => { });
        }
      });
    }

    return state;
  }
}
