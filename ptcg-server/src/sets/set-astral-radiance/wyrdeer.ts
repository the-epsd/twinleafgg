// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, DRAW_CARDS, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Wyrdeer extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Stantler';
  public cardType: CardType = P;
  public hp: number = 140;
  public weakness = [{ type: D }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C, C];

  public readonly HURRIED_GAIT_MARKER = 'WYRDEER_ASR_HURRIED_GAIT_MARKER';

  public powers = [  {
    name: 'Hurried Gait',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn, you may draw a card.'
  }];

  public attacks = [
    {
      name: 'Extrasensory',
      cost: [C, C],
      damage: 40,
      damageCalculation: '+' as '+',
      text: 'If you have the same number of cards in your hand as your opponent, this attack does 80 more damage.'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'ASR';
  public setNumber: string = '69';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Wyrdeer';
  public fullName: string = 'Wyrdeer (ASR 69)';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Hurried Gait - once per turn, draw a card
    // Ref: set-ex-deoxys/ludicolo.ts (Swing Dance - once per turn draw, marker to track usage)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.HURRIED_GAIT_MARKER, this);
      ABILITY_USED(player, this);
      DRAW_CARDS(player, 1);
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.HURRIED_GAIT_MARKER, this);

    // Attack 1: Extrasensory
    // Ref: set-astral-radiance/rampardos.ts (conditional damage based on hand count)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (player.hand.cards.length === opponent.hand.cards.length) {
        effect.damage += 80;
      }
    }

    return state;
  }
}
