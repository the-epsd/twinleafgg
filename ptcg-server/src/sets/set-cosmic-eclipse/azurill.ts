// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, EnergyType, SuperType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, GameError, GameMessage, PlayerType, SlotType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, COIN_FLIP_PROMPT, REMOVE_MARKER_AT_END_OF_TURN, ATTACH_ENERGY_PROMPT } from '../../game/store/prefabs/prefabs';

export class Azurill extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = Y;
  public hp: number = 60;
  public retreat = [];

  public readonly GROWING_UP_MARKER = 'AZURILL_CEC_GROWING_UP_MARKER';

  public powers = [{
    name: 'Growing Up',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may flip a coin. If heads, attach a basic Energy card from your discard pile to your Active Pokémon. If you use this Ability, your turn ends.'
  }];

  public set: string = 'CEC';
  public setNumber: string = '146';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Azurill';
  public fullName: string = 'Azurill CEC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Growing Up
    // Ref: set-unbroken-bonds/happiny.ts (Playhouse Heal - once per turn + turn ends)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.GROWING_UP_MARKER, this);
      ABILITY_USED(player, this);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          // Attach a basic Energy card from discard to active
          ATTACH_ENERGY_PROMPT(
            store,
            state,
            player,
            PlayerType.BOTTOM_PLAYER,
            SlotType.DISCARD,
            [SlotType.ACTIVE],
            { superType: SuperType.ENERGY, energyType: EnergyType.BASIC },
            { min: 0, max: 1, allowCancel: true }
          );
        }
        // Turn ends regardless of coin result
        const endTurnEffect = new EndTurnEffect(player);
        store.reduceEffect(state, endTurnEffect);
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.GROWING_UP_MARKER, this);

    return state;
  }
}
