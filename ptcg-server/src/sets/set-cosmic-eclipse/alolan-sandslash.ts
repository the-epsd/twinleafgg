// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, ADD_MARKER, PREVENT_DAMAGE_IF_TARGET_HAS_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class AlolanSandslash extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Alolan Sandshrew';
  public cardType: CardType = M;
  public hp: number = 120;
  public weakness = [{ type: R }];
  public resistance = [{ type: P, value: -20 }];
  public retreat = [C];

  public readonly CURVE_STRIKE_MARKER = 'ALOLAN_SANDSLASH_CEC_CURVE_STRIKE_MARKER';
  public readonly CLEAR_CURVE_STRIKE_MARKER = 'ALOLAN_SANDSLASH_CEC_CLEAR_CURVE_STRIKE_MARKER';

  public attacks = [
    {
      name: 'Curve Strike',
      cost: [],
      damage: 30,
      text: 'Flip a coin. If heads, prevent all damage done to this Pokémon by attacks during your opponent\'s next turn.'
    },
    {
      name: 'Reinforced Needle',
      cost: [M, C, C],
      damage: 60,
      damageCalculation: '+' as '+',
      text: 'If this Pokémon has a Pokémon Tool card attached to it, this attack does 60 more damage.'
    }
  ];

  public set: string = 'CEC';
  public setNumber: string = '138';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Alolan Sandslash';
  public fullName: string = 'Alolan Sandslash CEC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Curve Strike
    // Ref: set-unbroken-bonds/geodude.ts (Defense Curl)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          ADD_MARKER(this.CURVE_STRIKE_MARKER, player.active, this);
          ADD_MARKER(this.CLEAR_CURVE_STRIKE_MARKER, opponent, this);
        }
      });
    }

    // Attack 2: Reinforced Needle
    // Ref: AGENTS-patterns.md (tool check pattern)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      if (player.active.tools.length > 0) {
        effect.damage += 60;
      }
    }

    PREVENT_DAMAGE_IF_TARGET_HAS_MARKER(effect, this.CURVE_STRIKE_MARKER, this);
    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_CURVE_STRIKE_MARKER, this.CURVE_STRIKE_MARKER, this);

    return state;
  }
}
