// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SpecialCondition } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, COIN_FLIP_PROMPT, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { GameError, GameMessage } from '../../game';

export class Igglybuff extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 60;
  public retreat = [];

  public readonly SLEEPY_VOICE_MARKER = 'IGGLYBUFF_CEC_SLEEPY_VOICE_MARKER';

  public powers = [{
    name: 'Sleepy Voice',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may flip a coin. If heads, your opponent\'s Active Pokémon is now Asleep. If you use this Ability, your turn ends.'
  }];

  public set: string = 'CEC';
  public setNumber: string = '168';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Igglybuff';
  public fullName: string = 'Igglybuff CEC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Sleepy Voice
    // Ref: set-unbroken-bonds/happiny.ts (Playhouse Heal - once per turn + turn ends)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.SLEEPY_VOICE_MARKER, this);
      ABILITY_USED(player, this);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          opponent.active.addSpecialCondition(SpecialCondition.ASLEEP);
        }
        // Turn ends regardless of coin result
        const endTurnEffect = new EndTurnEffect(player);
        store.reduceEffect(state, endTurnEffect);
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.SLEEPY_VOICE_MARKER, this);

    return state;
  }
}
