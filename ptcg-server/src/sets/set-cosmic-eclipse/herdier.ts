// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { DealDamageEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, REMOVE_MARKER_AT_END_OF_TURN, REPLACE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Herdier extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Lillipup';
  public cardType: CardType = C;
  public hp: number = 100;
  public weakness = [{ type: F }];
  public retreat = [C, C];

  public readonly WORK_UP_MARKER = 'HERDIER_CEC_WORK_UP_MARKER';
  public readonly CLEAR_WORK_UP_MARKER = 'HERDIER_CEC_CLEAR_WORK_UP_MARKER';

  public attacks = [
    {
      name: 'Work Up',
      cost: [C],
      damage: 0,
      text: 'During your next turn, this Pokémon\'s attacks do 60 more damage to your opponent\'s Active Pokémon (before applying Weakness and Resistance).'
    },
    {
      name: 'Headbutt Bounce',
      cost: [C, C, C],
      damage: 60,
      text: ''
    }
  ];

  public set: string = 'CEC';
  public setNumber: string = '175';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Herdier';
  public fullName: string = 'Herdier CEC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Work Up
    // Ref: set-lost-thunder/gogoat.ts (Leaf Wallop - NEXT_TURN_ATTACK_BONUS pattern, manual version for all attacks)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      player.marker.addMarker(this.WORK_UP_MARKER, this);
    }

    // Apply bonus damage on this Pokemon's attacks during next turn
    if (effect instanceof DealDamageEffect && effect.source) {
      const player = effect.player;
      if ((player.marker.hasMarker(this.WORK_UP_MARKER, this) || player.marker.hasMarker(this.CLEAR_WORK_UP_MARKER, this))
        && effect.source.cards.includes(this)
        && effect.source.getPokemonCard() === this) {
        effect.damage += 60;
      }
    }

    // 2-phase marker cleanup for "during your next turn"
    if (effect instanceof EndTurnEffect) {
      REMOVE_MARKER_AT_END_OF_TURN(effect, this.CLEAR_WORK_UP_MARKER, this);
      REPLACE_MARKER_AT_END_OF_TURN(effect, this.WORK_UP_MARKER, this.CLEAR_WORK_UP_MARKER, this);
    }

    return state;
  }
}
