// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag, EnergyType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, PlayerType, EnergyCard } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckAttackCostEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Lycanroc extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Rockruff';
  public cardType: CardType = F;
  public hp: number = 120;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public powers = [{
    name: 'Boiling Blood',
    powerType: PowerType.ABILITY,
    text: 'If your opponent has any Pokémon-GX or Pokémon-EX in play, this Pokémon\'s attacks cost ColorlessColorlessColorless less.'
  }];

  public attacks = [
    {
      name: 'Voltage Claw',
      cost: [C, C, C],
      damage: 60,
      damageCalculation: '+' as '+',
      text: 'If your opponent\'s Active Pokémon has any Special Energy attached to it, this attack does 70 more damage.'
    }
  ];

  public set: string = 'CEC';
  public setNumber: string = '124';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Lycanroc';
  public fullName: string = 'Lycanroc CEC';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Boiling Blood (passive - CheckAttackCostEffect)
    // Ref: set-unified-minds/aerodactyl-gx.ts (Primal Winds - attack cost modification)
    if (effect instanceof CheckAttackCostEffect && effect.player.active.cards.includes(this)) {
      if (effect.player.active.getPokemonCard() !== this) {
        return state;
      }

      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      let hasGxOrEx = false;
      opponent.forEachPokemon(PlayerType.TOP_PLAYER, (cardList) => {
        const pokemonCard = cardList.getPokemonCard();
        if (pokemonCard && (pokemonCard.tags.includes(CardTag.POKEMON_GX) || pokemonCard.tags.includes(CardTag.POKEMON_EX))) {
          hasGxOrEx = true;
        }
      });

      if (hasGxOrEx) {
        // Remove up to 3 Colorless from cost
        let removed = 0;
        const newCost = [];
        for (const c of effect.cost) {
          if (c === CardType.COLORLESS && removed < 3) {
            removed++;
          } else {
            newCost.push(c);
          }
        }
        effect.cost = newCost;
      }
    }

    // Attack 1: Voltage Claw
    // Ref: AGENTS-patterns.md (special energy check for bonus damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const opponent = StateUtils.getOpponent(state, effect.player);
      const hasSpecialEnergy = opponent.active.cards.some(c =>
        c instanceof EnergyCard && c.energyType === EnergyType.SPECIAL
      );
      if (hasSpecialEnergy) {
        effect.damage += 70;
      }
    }

    return state;
  }
}
