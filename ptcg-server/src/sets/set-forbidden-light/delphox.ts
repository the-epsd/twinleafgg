// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, REMOVE_MARKER_AT_END_OF_TURN, ABILITY_USED, ADD_BURN_TO_PLAYER_ACTIVE } from '../../game/store/prefabs/prefabs';
import { DISCARD_X_ENERGY_FROM_THIS_POKEMON } from '../../game/store/prefabs/costs';

export class Delphox extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Braixen';
  public cardType: CardType = R;
  public hp: number = 150;
  public weakness = [{ type: W }];
  public retreat = [C, C];

  public readonly MYSTICAL_TORCH_MARKER = 'DELPHOX_FLI_MYSTICAL_TORCH_MARKER';

  public powers = [{
    name: 'Mystical Torch',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may leave your opponent\'s Active Pokémon Burned.'
  }];

  public attacks = [
    {
      name: 'Fire Spin',
      cost: [R, R, C, C],
      damage: 150,
      text: 'Discard 2 Energy from this Pokémon.'
    }
  ];

  public set: string = 'FLI';
  public setNumber: string = '17';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Delphox';
  public fullName: string = 'Delphox FLI';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Mystical Torch (activated - once per turn, burn opponent's active)
    // Refs: set-ultra-prism/leafeon-gx.ts (Breath of the Leaves - once per turn ability), set-guardians-rising/salazzle.ts (Hot Poison - burn)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.MYSTICAL_TORCH_MARKER, this);
      ABILITY_USED(player, this);

      ADD_BURN_TO_PLAYER_ACTIVE(store, state, opponent, this);
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.MYSTICAL_TORCH_MARKER, this);

    // Attack 1: Fire Spin
    // Ref: set-ultra-prism/magmar.ts (Flamethrower)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      DISCARD_X_ENERGY_FROM_THIS_POKEMON(store, state, effect, 2);
    }

    return state;
  }
}
