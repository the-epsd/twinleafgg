// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, SHOW_CARDS_TO_PLAYER, CONFIRMATION_PROMPT, SHUFFLE_DECK } from '../../game/store/prefabs/prefabs';

export class Pidove extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 60;
  public weakness = [{ type: L }];
  public resistance = [{ type: F, value: -20 }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Homing Pidove',
      cost: [C],
      damage: 0,
      text: 'Look at the top card of your deck. Then, you may shuffle your deck.'
    },
    {
      name: 'Gust',
      cost: [C, C],
      damage: 20,
      text: ''
    }
  ];

  public set: string = 'ROS';
  public setNumber: string = '78';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Pidove';
  public fullName: string = 'Pidove ROS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Homing Pidove
    // Ref: set-phantom-forces/munna.ts (See Through - SHOW_CARDS_TO_PLAYER pattern)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;

      if (player.deck.cards.length === 0) {
        return state;
      }

      const topCard = player.deck.cards[0];
      SHOW_CARDS_TO_PLAYER(store, state, player, [topCard]);

      CONFIRMATION_PROMPT(store, state, player, result => {
        if (result) {
          SHUFFLE_DECK(store, state, player);
        }
      });
    }

    return state;
  }
}
