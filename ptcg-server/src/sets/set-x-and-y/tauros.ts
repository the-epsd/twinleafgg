// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, THIS_POKEMON_DOES_DAMAGE_TO_ITSELF, MULTIPLE_COIN_FLIPS_PROMPT } from '../../game/store/prefabs/prefabs';

export class Tauros extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 110;
  public weakness = [{ type: F }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Take Down',
      cost: [C],
      damage: 30,
      text: 'This Pokémon does 10 damage to itself.'
    },
    {
      name: 'Seething Anger',
      cost: [C, C, C],
      damage: 30,
      damageCalculation: 'x' as 'x',
      text: 'Flip a coin for each damage counter on this Pokémon. This attack does 30 damage times the number of heads.'
    }
  ];

  public set: string = 'XY';
  public setNumber: string = '100';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Tauros';
  public fullName: string = 'Tauros XY';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ref: set-dragons-exalted/mareep.ts (self damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      THIS_POKEMON_DOES_DAMAGE_TO_ITSELF(store, state, effect, 10);
    }

    // Ref: set-boundaries-crossed/delcatty.ts (multiple coin flips pattern)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const damageCounters = Math.floor(player.active.damage / 10);
      if (damageCounters === 0) {
        effect.damage = 0;
        return state;
      }
      MULTIPLE_COIN_FLIPS_PROMPT(store, state, player, damageCounters, results => {
        effect.damage = 30 * results.filter(r => r).length;
      });
    }

    return state;
  }
}
