// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, REMOVE_MARKER_AT_END_OF_TURN, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED } from '../../game/store/prefabs/prefabs';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { GameError, GameMessage } from '../../game';

export class Clefable extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Clefairy';
  public cardType: CardType = P;
  public hp: number = 100;
  public weakness = [{ type: M }];
  public retreat = [C];

  public readonly LUNAR_BLESSING_MARKER = 'CLEFABLE_VIV_LUNAR_BLESSING_MARKER';

  public powers = [{
    name: 'Lunar Blessing',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn, if your Active Pokémon has any [P] Energy attached, you may heal 20 damage from it, and it recovers from a Special Condition.'
  }];

  public attacks = [
    {
      name: 'Magical Shot',
      cost: [P, C, C],
      damage: 80,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'VIV';
  public setNumber: string = '64';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Clefable';
  public fullName: string = 'Clefable VIV';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Lunar Blessing
    // Ref: set-vivid-voltage/celebi.ts (CheckProvidedEnergyEffect for energy check),
    //      set-darkness-ablaze/toxapex.ts (direct damage property modification for heal)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      // Check if Active Pokemon has any [P] Energy attached
      const checkEnergy = new CheckProvidedEnergyEffect(player, player.active);
      store.reduceEffect(state, checkEnergy);
      const hasPsychicEnergy = checkEnergy.energyMap.some(em =>
        em.provides.some(t => t === CardType.PSYCHIC || t === CardType.ANY)
      );

      if (!hasPsychicEnergy) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.LUNAR_BLESSING_MARKER, this);
      ABILITY_USED(player, this);

      // Heal 20 damage from Active Pokemon
      player.active.damage = Math.max(0, player.active.damage - 20);

      // Remove all Special Conditions
      player.active.specialConditions = [];
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.LUNAR_BLESSING_MARKER, this);

    return state;
  }
}
