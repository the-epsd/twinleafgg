// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { TrainerEffect } from '../../game/store/effects/play-card-effects';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_PARALYZED } from '../../game/store/prefabs/attack-effects';

export class Drednaw extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Chewtle';
  public cardType: CardType = W;
  public hp: number = 140;
  public weakness = [{ type: L }];
  public retreat = [C, C, C, C];

  public attacks = [
    {
      name: 'Vise Wave',
      cost: [W, C, C],
      damage: 80,
      text: 'If you played Nessa from your hand during this turn, your opponent\'s Active Pokémon is now Paralyzed.'
    },
    {
      name: 'Surf',
      cost: [W, W, C, C],
      damage: 140,
      text: ''
    }
  ];

  public readonly NESSA_MARKER = 'DREDNAW_VIV_NESSA_MARKER';

  public regulationMark: string = 'D';
  public set: string = 'VIV';
  public setNumber: string = '39';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Drednaw';
  public fullName: string = 'Drednaw VIV';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Track if Nessa was played this turn
    // Ref: set-ultra-prism/garchomp.ts (Royal Blades - track specific supporter played)
    if (effect instanceof TrainerEffect && effect.trainerCard.name === 'Nessa') {
      const player = effect.player;
      ADD_MARKER(this.NESSA_MARKER, player, this);
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.NESSA_MARKER, this);

    // Attack 1: Vise Wave
    // Ref: set-ultra-prism/garchomp.ts (Royal Blades - check specific supporter marker)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      if (HAS_MARKER(this.NESSA_MARKER, effect.player, this)) {
        YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_PARALYZED(store, state, effect);
      }
    }

    return state;
  }
}
