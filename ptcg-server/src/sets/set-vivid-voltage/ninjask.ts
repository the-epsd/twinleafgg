// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, JUST_EVOLVED, IS_ABILITY_BLOCKED, SEARCH_YOUR_DECK_FOR_POKEMON_AND_PUT_ONTO_BENCH, HEAL_X_DAMAGE_FROM_THIS_POKEMON } from '../../game/store/prefabs/prefabs';

export class Ninjask extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Nincada';
  public cardType: CardType = G;
  public hp: number = 60;
  public weakness = [{ type: R }];
  public retreat = [];

  public powers = [  {
    name: 'Cast-off Shell',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand to evolve 1 of your Pokémon, you may search your deck for Shedinja and put it onto your Bench. Shuffle your deck afterward.'
  }];

  public attacks = [
    {
      name: 'Absorb',
      cost: [G],
      damage: 30,
      text: 'Heal 30 damage from this Pokémon.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'VIV';
  public setNumber: string = '14';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ninjask';
  public fullName: string = 'Ninjask VIV';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Cast-off Shell
    // Ref: set-vivid-voltage/alcremie.ts (JUST_EVOLVED + IS_ABILITY_BLOCKED), set-celestial-storm/scyther.ts (SEARCH_YOUR_DECK_FOR_POKEMON_AND_PUT_ONTO_BENCH)
    if (JUST_EVOLVED(effect, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      SEARCH_YOUR_DECK_FOR_POKEMON_AND_PUT_ONTO_BENCH(store, state, player, { name: 'Shedinja' }, { min: 0, max: 1, allowCancel: true });
    }

    // Attack 1: Absorb
    // Ref: set-darkness-ablaze/toxapex.ts (heal all damage = damage = 0 pattern), AGENTS-patterns.md
    if (WAS_ATTACK_USED(effect, 0, this)) {
      HEAL_X_DAMAGE_FROM_THIS_POKEMON(effect, store, state, 30);
    }

    return state;
  }
}
