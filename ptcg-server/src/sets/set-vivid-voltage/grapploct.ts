// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, BLOCK_RETREAT, BLOCK_RETREAT_IF_MARKER, REMOVE_MARKER_FROM_ACTIVE_AT_END_OF_TURN, REPLACE_MARKER_AT_END_OF_TURN, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { MarkerConstants } from '../../game/store/markers/marker-constants';

export class Grapploct extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Clobbopus';
  public cardType: CardType = F;
  public hp: number = 130;
  public weakness = [{ type: P }];
  public retreat = [C, C];

  public readonly FULL_NELSON_MARKER = 'GRAPPLOCT_VIV_FULL_NELSON_MARKER';
  public readonly CLEAR_FULL_NELSON_MARKER = 'GRAPPLOCT_VIV_CLEAR_FULL_NELSON_MARKER';

  public attacks = [
    {
      name: 'Full Nelson',
      cost: [F],
      damage: 30,
      text: 'During your opponent\'s next turn, the Defending Pokémon can\'t retreat.'
    },
    {
      name: 'Tentacle Buster',
      cost: [F, C],
      damage: 50,
      damageCalculation: '+' as '+',
      text: 'If this Pokémon used Full Nelson during your last turn, this attack does 120 more damage.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'VIV';
  public setNumber: string = '101';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Grapploct';
  public fullName: string = 'Grapploct VIV';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Full Nelson
    // Ref: set-champions-path/scrafty.ts (BLOCK_RETREAT 3-call pattern for opponent's next turn)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      // Track that Full Nelson was used (for Tentacle Buster bonus)
      player.marker.addMarker(this.FULL_NELSON_MARKER, this);
      return BLOCK_RETREAT(store, state, effect, this);
    }

    BLOCK_RETREAT_IF_MARKER(effect, MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);
    REMOVE_MARKER_FROM_ACTIVE_AT_END_OF_TURN(effect, MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);

    // Attack 2: Tentacle Buster
    // Ref: set-sword-and-shield/galarian-meowth.ts (2-marker pattern for "during your last turn")
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      if (player.marker.hasMarker(this.FULL_NELSON_MARKER, this) ||
          player.marker.hasMarker(this.CLEAR_FULL_NELSON_MARKER, this)) {
        effect.damage += 120;
      }
    }

    // 2-phase marker cleanup for Full Nelson bonus tracking
    if (effect instanceof EndTurnEffect) {
      REMOVE_MARKER_AT_END_OF_TURN(effect, this.CLEAR_FULL_NELSON_MARKER, this);
      REPLACE_MARKER_AT_END_OF_TURN(effect, this.FULL_NELSON_MARKER, this.CLEAR_FULL_NELSON_MARKER, this);
    }

    return state;
  }
}
