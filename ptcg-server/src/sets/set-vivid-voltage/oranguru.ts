// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, ChooseCardsPrompt, GameMessage, GameError, CardList } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Oranguru extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 120;
  public weakness = [{ type: F }];
  public retreat = [C, C];

  public readonly PRIMATE_WISDOM_MARKER = 'ORANGURU_VIV_PRIMATE_WISDOM_MARKER';

  public powers = [  {
    name: 'Primate Wisdom',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn, you may switch a card from your hand with the top card of your deck.'
  }];

  public attacks = [
    {
      name: 'Whap Down',
      cost: [C, C, C],
      damage: 70,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'VIV';
  public setNumber: string = '199';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Oranguru';
  public fullName: string = 'Oranguru VIV';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Primate Wisdom
    // Ref: set-legends-awakened/uxie-lv-x.ts (ADD_MARKER + ChooseCardsPrompt from deckTop),
    //      set-vivid-voltage/clefable.ts (IS_ABILITY_BLOCKED + USE_ABILITY_ONCE_PER_TURN + ABILITY_USED)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      if (player.hand.cards.length === 0) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      if (player.deck.cards.length === 0) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.PRIMATE_WISDOM_MARKER, this);
      ABILITY_USED(player, this);

      // Take the top card of the deck into a temp list
      const deckTop = new CardList();
      player.deck.moveTo(deckTop, 1);

      // Player chooses 1 card from their hand to put on top of deck
      return store.prompt(state, new ChooseCardsPrompt(
        player,
        GameMessage.CHOOSE_CARD_TO_DECK,
        player.hand,
        {},
        { min: 1, max: 1, allowCancel: false }
      ), selected => {
        const cards = selected || [];
        if (cards.length > 0) {
          // Move chosen hand card to top of deck
          player.hand.moveCardsTo(cards, player.deck);
          // Move the former top card into hand
          deckTop.moveTo(player.hand);
        } else {
          // Cancelled: move deckTop back
          deckTop.moveToTopOfDestination(player.deck);
        }
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.PRIMATE_WISDOM_MARKER, this);

    return state;
  }
}
