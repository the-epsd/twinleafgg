// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag } from '../../game/store/card/card-types';
import { SlotType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, THIS_ATTACK_DOES_X_DAMAGE_TO_X_OF_YOUR_OPPONENTS_POKEMON, THIS_POKEMON_CANNOT_USE_THIS_ATTACK_NEXT_TURN } from '../../game/store/prefabs/prefabs';

export class SalamenceVmax extends PokemonCard {
  public tags = [CardTag.POKEMON_VMAX];
  public stage: Stage = Stage.VMAX;
  public evolvesFrom: string = 'Salamence V';
  public cardType: CardType = C;
  public hp: number = 320;
  public weakness = [{ type: L }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Sonic Double',
      cost: [C],
      damage: 0,
      text: 'This attack does 40 damage to 2 of your opponent\'s Pokémon. (Don\'t apply Weakness and Resistance for Benched Pokémon.)'
    },
    {
      name: 'Max Wings',
      cost: [C, C, C, C],
      damage: 240,
      text: 'During your next turn, this Pokémon can\'t use Max Wings.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'DAA';
  public setNumber: string = '144';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Salamence VMAX';
  public fullName: string = 'Salamence VMAX DAA';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Sonic Double
    // Ref: set-darkness-ablaze/salamence-v.ts (Swoop Across - DealDamageEffect for active, PutDamageEffect for bench)
    // but with player choosing 2 targets - use THIS_ATTACK_DOES_X_DAMAGE_TO_X_OF_YOUR_OPPONENTS_POKEMON with ACTIVE+BENCH
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      const allTargets = [opponent.active, ...opponent.bench.filter(b => b.cards.length > 0)];
      const count = Math.min(2, allTargets.length);

      THIS_ATTACK_DOES_X_DAMAGE_TO_X_OF_YOUR_OPPONENTS_POKEMON(40, effect, store, state, count, count, false, [SlotType.ACTIVE, SlotType.BENCH]);
    }

    // Attack 2: Max Wings
    // Ref: prefabs.ts (THIS_POKEMON_CANNOT_USE_THIS_ATTACK_NEXT_TURN)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      THIS_POKEMON_CANNOT_USE_THIS_ATTACK_NEXT_TURN(effect.player, this.attacks[1]);
    }

    return state;
  }
}
