// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { Player, PlayerType, PowerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';
import { CheckPokemonStatsEffect } from '../../game/store/effects/check-effects';

export class Solrock extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = F;
  public hp: number = 90;
  public weakness = [{ type: G }];
  public retreat = [C];

  public powers = [{
    name: 'Resistance Shade',
    powerType: PowerType.ABILITY,
    text: 'If you have Lunatone in play, your opponent\'s Pokémon in play have no Resistance.'
  }];

  public attacks = [
    {
      name: 'Rock Throw',
      cost: [F, C],
      damage: 30,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'DAA';
  public setNumber: string = '92';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Solrock';
  public fullName: string = 'Solrock DAA';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Resistance Shade (passive - remove resistance from opponent's Pokemon if Lunatone in play)
    // Ref: set-forbidden-light/mesprit.ts (Silent Waves - CheckPokemonStatsEffect + effect.resistance = [])
    if (effect instanceof CheckPokemonStatsEffect) {
      const targetOwner = StateUtils.findOwner(state, effect.target);

      // Find this Solrock's owner
      let solrockOwner: Player | null = null;
      const players = [targetOwner, StateUtils.getOpponent(state, targetOwner)];
      players.forEach(p => {
        p.forEachPokemon(PlayerType.BOTTOM_PLAYER, cardList => {
          if (cardList.getPokemonCard() === this) {
            solrockOwner = p;
          }
        });
      });

      // Ability only removes resistance from OPPONENT's Pokemon
      if (!solrockOwner || solrockOwner === targetOwner) {
        return state;
      }

      if (IS_ABILITY_BLOCKED(store, state, solrockOwner, this)) {
        return state;
      }

      // Check if Lunatone is in play on Solrock's owner's side
      let hasLunatone = false;
      const owner = solrockOwner as Player;
      owner.forEachPokemon(PlayerType.BOTTOM_PLAYER, cardList => {
        if (cardList.getPokemonCard()?.name === 'Lunatone') {
          hasLunatone = true;
        }
      });

      if (hasLunatone) {
        effect.resistance = [];
      }
    }

    return state;
  }
}
