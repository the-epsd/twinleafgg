// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, DISCARD_TOP_X_CARDS_FROM_YOUR_DECK, DISCARD_TOP_X_OF_OPPONENTS_DECK, THIS_POKEMON_DOES_DAMAGE_TO_ITSELF } from '../../game/store/prefabs/prefabs';

export class Toxtricity extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Toxel';
  public cardType: CardType = L;
  public hp: number = 120;
  public weakness = [{ type: F }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Risk Taker',
      cost: [C, C],
      damage: 0,
      text: 'Flip a coin. If heads, discard the top 5 cards of your opponent\'s deck. If tails, discard the top 5 cards of your deck.'
    },
    {
      name: 'Thunder Jolt',
      cost: [L, L, C],
      damage: 160,
      text: 'This Pokémon also does 30 damage to itself.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'DAA';
  public setNumber: string = '63';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Toxtricity';
  public fullName: string = 'Toxtricity DAA';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Risk Taker
    // Ref: prefabs.ts (COIN_FLIP_PROMPT + DISCARD_TOP_X_OF_OPPONENTS_DECK / DISCARD_TOP_X_CARDS_FROM_YOUR_DECK)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          // Heads: discard top 5 of opponent's deck
          DISCARD_TOP_X_OF_OPPONENTS_DECK(store, state, player, 5, this, this.attacks[0]);
        } else {
          // Tails: discard top 5 of your deck
          DISCARD_TOP_X_CARDS_FROM_YOUR_DECK(store, state, player, 5, this, this.attacks[0]);
        }
      });
    }

    // Attack 2: Thunder Jolt
    // Ref: prefabs.ts (THIS_POKEMON_DOES_DAMAGE_TO_ITSELF)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      THIS_POKEMON_DOES_DAMAGE_TO_ITSELF(store, state, effect, 30);
    }

    return state;
  }
}
