// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils, GameMessage, PlayerType, SlotType } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PutDamageEffect } from '../../game/store/effects/attack-effects';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { ChoosePokemonPrompt } from '../../game/store/prompts/choose-pokemon-prompt';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Luxray extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Luxio';
  public cardType: CardType = L;
  public hp: number = 150;
  public weakness = [{ type: F }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Energy Crush',
      cost: [L],
      damage: 50,
      damageCalculation: 'x' as 'x',
      text: 'This attack does 50 damage for each Energy attached to all of your opponent\'s Pokémon.'
    },
    {
      name: 'Flash Impact',
      cost: [L],
      damage: 110,
      text: 'This attack also does 30 damage to 1 of your Benched Pokémon. (Don\'t apply Weakness and Resistance for Benched Pokémon.)'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'BRS';
  public setNumber: string = '51';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Luxray';
  public fullName: string = 'Luxray BRS 51';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Energy Crush
    // Ref: set-astral-radiance/hisuian-samurott.ts (Dark Mastery - energy count across all Pokemon)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      let totalEnergy = 0;
      opponent.forEachPokemon(PlayerType.TOP_PLAYER, (cardList) => {
        const checkProvided = new CheckProvidedEnergyEffect(opponent, cardList);
        store.reduceEffect(state, checkProvided);
        checkProvided.energyMap.forEach(em => {
          totalEnergy += em.provides.length;
        });
      });

      effect.damage = 50 * totalEnergy;
    }

    // Attack 2: Flash Impact
    // Ref: set-next-destinies/luxray.ts (Flash Impact - damage to own benched)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const myBenched = player.bench.filter(b => b.cards.length > 0);

      if (myBenched.length > 0) {
        return store.prompt(state, new ChoosePokemonPrompt(
          player.id,
          GameMessage.CHOOSE_POKEMON_TO_DAMAGE,
          PlayerType.BOTTOM_PLAYER,
          [SlotType.BENCH],
          { min: 1, max: 1, allowCancel: false }
        ), selected => {
          if (selected && selected.length > 0) {
            const damageEffect = new PutDamageEffect(effect, 30);
            damageEffect.target = selected[0];
            store.reduceEffect(state, damageEffect);
          }
        });
      }
    }

    return state;
  }
}
