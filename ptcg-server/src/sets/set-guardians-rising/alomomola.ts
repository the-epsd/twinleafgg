// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SuperType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils, GameMessage, Card, ChooseCardsPrompt } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Alomomola extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = W;
  public hp: number = 110;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Borne Ashore',
      cost: [C],
      damage: 0,
      text: 'Put a Basic Pokémon from either player\'s discard pile onto its owner\'s Bench.'
    },
    {
      name: 'Hydro Splash',
      cost: [W, C, C],
      damage: 60,
      text: ''
    }
  ];

  public set: string = 'GRI';
  public setNumber: string = '36';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Alomomola';
  public fullName: string = 'Alomomola GRI';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Borne Ashore
    // Ref: set-astral-radiance/mantine.ts (Borne Ashore)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      // Check if any basic Pokemon exist in either discard pile
      const playerHasBasics = player.discard.cards.some(c =>
        c instanceof PokemonCard && c.stage === Stage.BASIC
      );
      const opponentHasBasics = opponent.discard.cards.some(c =>
        c instanceof PokemonCard && c.stage === Stage.BASIC
      );

      if (!playerHasBasics && !opponentHasBasics) {
        return state;
      }

      // Check if either player has bench space
      const playerHasSpace = player.bench.some(b => b.cards.length === 0);
      const opponentHasSpace = opponent.bench.some(b => b.cards.length === 0);

      // Build combined list of options
      // Try player's discard if they have space
      if (playerHasBasics && playerHasSpace) {
        let cards: Card[] = [];
        return store.prompt(state, new ChooseCardsPrompt(
          player,
          GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH,
          player.discard,
          { superType: SuperType.POKEMON, stage: Stage.BASIC },
          { min: 1, max: 1, allowCancel: opponentHasBasics && opponentHasSpace }
        ), selected => {
          cards = selected || [];
          if (cards.length > 0) {
            const slot = player.bench.find(b => b.cards.length === 0);
            if (slot) {
              player.discard.moveCardTo(cards[0], slot);
              slot.pokemonPlayedTurn = state.turn;
            }
          } else if (opponentHasBasics && opponentHasSpace) {
            // Player cancelled, try opponent's discard
            store.prompt(state, new ChooseCardsPrompt(
              player,
              GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH,
              opponent.discard,
              { superType: SuperType.POKEMON, stage: Stage.BASIC },
              { min: 1, max: 1, allowCancel: false }
            ), selected2 => {
              const cards2 = selected2 || [];
              if (cards2.length > 0) {
                const slot = opponent.bench.find(b => b.cards.length === 0);
                if (slot) {
                  opponent.discard.moveCardTo(cards2[0], slot);
                  slot.pokemonPlayedTurn = state.turn;
                }
              }
            });
          }
        });
      }

      // Only opponent's discard has basics (and opponent has space)
      if (opponentHasBasics && opponentHasSpace) {
        return store.prompt(state, new ChooseCardsPrompt(
          player,
          GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH,
          opponent.discard,
          { superType: SuperType.POKEMON, stage: Stage.BASIC },
          { min: 1, max: 1, allowCancel: false }
        ), selected => {
          const cards = selected || [];
          if (cards.length > 0) {
            const slot = opponent.bench.find(b => b.cards.length === 0);
            if (slot) {
              opponent.discard.moveCardTo(cards[0], slot);
              slot.pokemonPlayedTurn = state.turn;
            }
          }
        });
      }
    }

    return state;
  }
}
