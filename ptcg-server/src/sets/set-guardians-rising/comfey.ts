// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, PlayerType } from '../../game';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, PREVENT_AND_CLEAR_SPECIAL_CONDITIONS, DRAW_CARDS } from '../../game/store/prefabs/prefabs';

export class Comfey extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = Y;
  public hp: number = 70;
  public weakness = [{ type: M }];
  public resistance = [{ type: D, value: -20 }];
  public retreat = [C];

  public powers = [  {
    name: 'Flower Shield',
    powerType: PowerType.ABILITY,
    text: 'Each of your Pokémon that has any [Y] Energy attached to it can\'t be affected by any Special Conditions. Remove any Special Conditions affecting those Pokémon.'
  }];

  public attacks = [
    {
      name: 'Sweet Kiss',
      cost: [Y, C],
      damage: 30,
      text: 'Your opponent draws a card.'
    }
  ];

  public set: string = 'GRI';
  public setNumber: string = '93';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Comfey';
  public fullName: string = 'Comfey GRI';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Flower Shield (passive)
    // Ref: set-x-and-y/slurpuff.ts (Sweet Veil)
    PREVENT_AND_CLEAR_SPECIAL_CONDITIONS(state, effect, {
      shouldApply: (target, owner) => {
        let isInPlay = false;
        owner.forEachPokemon(PlayerType.BOTTOM_PLAYER, cardList => {
          if (cardList.getPokemonCard() === this) {
            isInPlay = true;
          }
        });

        if (!isInPlay) {
          return false;
        }

        if (IS_ABILITY_BLOCKED(store, state, owner, this)) {
          return false;
        }

        const checkEnergy = new CheckProvidedEnergyEffect(owner, target);
        store.reduceEffect(state, checkEnergy);

        return checkEnergy.energyMap.some(em =>
          em.provides.includes(CardType.FAIRY)
        );
      }
    });

    // Attack 1: Sweet Kiss
    // Ref: AGENTS-patterns.md (opponent draws cards)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      DRAW_CARDS(opponent, 1);
    }

    return state;
  }
}
