// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, ConfirmPrompt, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, JUST_EVOLVED, IS_ABILITY_BLOCKED, ADD_BURN_TO_PLAYER_ACTIVE, ADD_POISON_TO_PLAYER_ACTIVE } from '../../game/store/prefabs/prefabs';
import { DISCARD_X_ENERGY_FROM_THIS_POKEMON } from '../../game/store/prefabs/costs';

export class Salazzle extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Salandit';
  public cardType: CardType = R;
  public hp: number = 110;
  public weakness = [{ type: W }];
  public retreat = [C];

  public powers = [{
    name: 'Hot Poison',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand to evolve 1 of your Pokémon during your turn, you may leave your opponent\'s Active Pokémon Burned and Poisoned.'
  }];

  public attacks = [
    {
      name: 'Flamethrower',
      cost: [R, C, C],
      damage: 90,
      text: 'Discard an Energy from this Pokémon.'
    }
  ];

  public set: string = 'GRI';
  public setNumber: string = '16';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Salazzle';
  public fullName: string = 'Salazzle GRI';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Hot Poison (on-evolve)
    // Ref: set-sun-and-moon/tsareena.ts (Queenly Majesty)
    if (JUST_EVOLVED(effect, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const opponent = StateUtils.getOpponent(state, player);

      state = store.prompt(state, new ConfirmPrompt(
        player.id,
        GameMessage.WANT_TO_USE_ABILITY
      ), wantToUse => {
        if (wantToUse) {
          ADD_BURN_TO_PLAYER_ACTIVE(store, state, opponent, this);
          ADD_POISON_TO_PLAYER_ACTIVE(store, state, opponent, this);
        }
      });
    }

    // Attack 1: Flamethrower
    // Ref: set-sun-and-moon/torracat.ts (Flamethrower)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      DISCARD_X_ENERGY_FROM_THIS_POKEMON(store, state, effect, 1);
    }

    return state;
  }
}
