// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AfterAttackEffect, EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN, DRAW_CARDS, SWITCH_ACTIVE_WITH_BENCHED, BLOCK_IF_DECK_EMPTY } from '../../game/store/prefabs/prefabs';

export class AlolanSandslash extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Alolan Sandshrew';
  public cardType: CardType = W;
  public hp: number = 110;
  public weakness = [{ type: M }];
  public retreat = [C, C];

  public readonly SLUSH_RUSH_MARKER = 'ALOLAN_SANDSLASH_SLUSH_RUSH_MARKER';

  public powers = [{
    name: 'Slush Rush',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may draw a card.'
  }];

  public attacks = [
    {
      name: 'Smash Turn',
      cost: [W, C, C],
      damage: 50,
      text: 'Switch this Pokémon with 1 of your Benched Pokémon.'
    }
  ];

  public set: string = 'GRI';
  public setNumber: string = '20';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Alolan Sandslash';
  public fullName: string = 'Alolan Sandslash GRI';

  public usedSmashTurn = false;

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Slush Rush
    // Ref: set-sun-and-moon/gumshoos-gx.ts (Search the Premises - once per turn ability)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      BLOCK_IF_DECK_EMPTY(player);
      USE_ABILITY_ONCE_PER_TURN(player, this.SLUSH_RUSH_MARKER, this);
      ABILITY_USED(player, this);

      DRAW_CARDS(player, 1);
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.SLUSH_RUSH_MARKER, this);

    // Attack 1: Smash Turn
    // Ref: AGENTS-patterns.md (post-damage switch with AfterAttackEffect)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      this.usedSmashTurn = true;
    }

    if (effect instanceof AfterAttackEffect && this.usedSmashTurn) {
      this.usedSmashTurn = false;
      const player = effect.player;

      if (player.bench.some(b => b.cards.length > 0)) {
        SWITCH_ACTIVE_WITH_BENCHED(store, state, player);
      }
    }

    if (effect instanceof EndTurnEffect) {
      this.usedSmashTurn = false;
    }

    return state;
  }
}
