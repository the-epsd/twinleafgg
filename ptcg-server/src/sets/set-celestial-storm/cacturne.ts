// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, GameMessage, ChoosePokemonPrompt, PlayerType, SlotType } from '../../game';
import { AfterDamageEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { GamePhase } from '../../game/store/state/state';
import { WAS_ATTACK_USED, ADD_POISON_TO_PLAYER_ACTIVE, IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Cacturne extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Cacnea';
  public cardType: CardType = G;
  public hp: number = 110;
  public weakness = [{ type: R }];
  public retreat = [C];

  public powers = [{
    name: 'Poison Payback',
    powerType: PowerType.ABILITY,
    text: 'If this Pokémon is your Active Pokémon and is damaged by an opponent\'s attack (even if this Pokémon is Knocked Out), the Attacking Pokémon is now Poisoned.'
  }];

  public attacks = [
    {
      name: 'Feint Attack',
      cost: [C, C, C],
      damage: 0,
      text: 'This attack does 50 damage to 1 of your opponent\'s Pokémon. This damage isn\'t affected by Weakness, Resistance, or any other effects on that Pokémon.'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '20';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Cacturne';
  public fullName: string = 'Cacturne CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Poison Payback (passive - triggered when damaged by opponent's attack)
    // Ref: set-forbidden-light/dragalge.ts (Poison Point)
    if (effect instanceof AfterDamageEffect && effect.target.getPokemonCard() === this && state.phase === GamePhase.ATTACK) {
      const player = StateUtils.findOwner(state, effect.target);
      const opponent = effect.player;

      if (player === opponent || player.active !== effect.target) {
        return state;
      }
      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      ADD_POISON_TO_PLAYER_ACTIVE(store, state, opponent, this);
    }

    // Attack 1: Feint Attack
    // Ref: set-guardians-rising/honchkrow.ts (Feint Attack)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;

      return store.prompt(state, new ChoosePokemonPrompt(
        player.id,
        GameMessage.CHOOSE_POKEMON_TO_DAMAGE,
        PlayerType.TOP_PLAYER,
        [SlotType.ACTIVE, SlotType.BENCH],
        { min: 1, max: 1, allowCancel: false }
      ), selected => {
        const target = selected[0];
        target.damage += 50;
        const afterDamage = new AfterDamageEffect(effect, 50);
        state = store.reduceEffect(state, afterDamage);
      });
    }

    return state;
  }
}
