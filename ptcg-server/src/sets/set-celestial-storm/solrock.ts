// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PlayerType, PowerType, StoreLike, State } from '../../game';
import { CheckHpEffect } from '../../game/store/effects/check-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, COIN_FLIP_PROMPT } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_PARALYZED, YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_BURNED } from '../../game/store/prefabs/attack-effects';

export class Solrock extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = P;
  public hp: number = 90;
  public weakness = [{ type: P }];
  public retreat = [C];

  public powers = [{
    name: 'Sunbeam',
    powerType: PowerType.ABILITY,
    text: 'The maximum HP of each of your Lunatone in play is 130.'
  }];

  public attacks = [
    {
      name: 'Scorching Light',
      cost: [C],
      damage: 0,
      text: 'Flip a coin. If heads, your opponent\'s Active Pokémon is now Paralyzed. If tails, your opponent\'s Active Pokémon is now Burned.'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '62';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Solrock';
  public fullName: string = 'Solrock CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Sunbeam (passive - modify HP of Lunatone)
    // Ref: set-fossil/ditto.ts (CheckHpEffect HP modification)
    if (effect instanceof CheckHpEffect) {
      const targetCard = effect.target.getPokemonCard();
      if (targetCard && targetCard.name === 'Lunatone') {
        // Check if Solrock is in play on the same side
        const player = effect.player;
        let solrockInPlay = false;
        player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card) => {
          if (card === this) {
            solrockInPlay = true;
          }
        });

        if (solrockInPlay && !IS_ABILITY_BLOCKED(store, state, player, this)) {
          effect.hp = 130;
        }
      }
    }

    // Attack 1: Scorching Light
    // Ref: AGENTS-patterns.md (coin flip with heads/tails outcomes)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      COIN_FLIP_PROMPT(store, state, effect.player, result => {
        if (result) {
          YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_PARALYZED(store, state, effect);
        } else {
          YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_BURNED(store, state, effect);
        }
      });
    }

    return state;
  }
}
