// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, THIS_POKEMON_DOES_DAMAGE_TO_ITSELF, LOOK_AT_TOP_X_CARDS_AND_ATTACH_UP_TO_Y_ENERGY } from '../../game/store/prefabs/prefabs';

export class Heatran extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = M;
  public hp: number = 130;
  public weakness = [{ type: R }];
  public resistance = [{ type: P, value: -20 }];
  public retreat = [C, C, C];

  public attacks = [
    {
      name: 'Steelworks',
      cost: [M],
      damage: 0,
      text: 'Look at the top 4 cards of your deck and attach any number of [M] Energy cards you find there to 1 of your Pokémon. Shuffle the other cards back into your deck.'
    },
    {
      name: 'Steel Tackle',
      cost: [M, C, C, C],
      damage: 120,
      text: 'This Pokémon does 30 damage to itself.'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '98';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Heatran';
  public fullName: string = 'Heatran CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Steelworks
    // Ref: set-temporal-forces/metang.ts (Metal Maker - LOOK_AT_TOP_X_CARDS_AND_ATTACH_UP_TO_Y_ENERGY)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      return LOOK_AT_TOP_X_CARDS_AND_ATTACH_UP_TO_Y_ENERGY(
        store, state, player, 4, 4,
        {
          validCardTypes: [CardType.METAL],
          sameTarget: true,
          remainderDestination: 'shuffle'
        }
      );
    }

    // Attack 2: Steel Tackle
    // Ref: set-celestial-storm/bagon-2.ts (Reckless Charge)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      THIS_POKEMON_DOES_DAMAGE_TO_ITSELF(store, state, effect, 30);
    }

    return state;
  }
}
