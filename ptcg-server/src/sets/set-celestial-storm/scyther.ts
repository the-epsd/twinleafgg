// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { AbstractAttackEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, ADD_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN, SEARCH_YOUR_DECK_FOR_POKEMON_AND_PUT_ONTO_BENCH } from '../../game/store/prefabs/prefabs';

export class Scyther extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = G;
  public hp: number = 70;
  public weakness = [{ type: R }];
  public retreat = [C];

  public readonly AGILITY_MARKER = 'SCYTHER_CES_AGILITY_MARKER';
  public readonly CLEAR_AGILITY_MARKER = 'SCYTHER_CES_CLEAR_AGILITY_MARKER';

  public attacks = [
    {
      name: 'Twin Play',
      cost: [C],
      damage: 0,
      text: 'Search your deck for up to 2 Scyther and put them onto your Bench. Then, shuffle your deck.'
    },
    {
      name: 'Agility',
      cost: [C, C],
      damage: 20,
      text: 'Flip a coin. If heads, prevent all effects of attacks, including damage, done to this Pokémon during your opponent\'s next turn.'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '4';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Scyther';
  public fullName: string = 'Scyther CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Twin Play
    // Ref: set-forbidden-light/exeggcute.ts (Multiply)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      return SEARCH_YOUR_DECK_FOR_POKEMON_AND_PUT_ONTO_BENCH(store, state, player, { name: 'Scyther' }, { max: 2 });
    }

    // Attack 2: Agility
    // Ref: set-ultra-prism/yanma.ts (Super Speed - prevent all effects of attacks with coin flip)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          ADD_MARKER(this.AGILITY_MARKER, player.active, this);
          ADD_MARKER(this.CLEAR_AGILITY_MARKER, opponent, this);
        }
      });
    }

    // Prevent all effects of attacks (including damage) done to this Pokemon
    if (effect instanceof AbstractAttackEffect
      && effect.target.marker.hasMarker(this.AGILITY_MARKER, this)
      && effect.target.getPokemonCard() === this) {
      effect.preventDefault = true;
      return state;
    }

    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_AGILITY_MARKER, this.AGILITY_MARKER, this);

    return state;
  }
}
