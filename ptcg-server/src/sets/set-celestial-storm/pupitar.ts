// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PlayerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckPokemonTypeEffect } from '../../game/store/effects/check-effects';
import { DealDamageEffect, PutDamageEffect } from '../../game/store/effects/attack-effects';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Pupitar extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Larvitar';
  public cardType: CardType = F;
  public hp: number = 80;
  public weakness = [{ type: G }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Skull Bash',
      cost: [C, C],
      damage: 20,
      text: ''
    },
    {
      name: 'Dust Devil',
      cost: [F, F],
      damage: 0,
      text: 'This attack does 20 damage to each non-Fighting Pokémon (both yours and your opponent\'s). (Don\'t apply Weakness and Resistance for Benched Pokémon.)'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '75';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Pupitar';
  public fullName: string = 'Pupitar CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 2: Dust Devil
    // Refs: set-ultra-prism/bronzong.ts (CheckPokemonTypeEffect), AGENTS-patterns.md (damage to all Pokemon)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      // Helper to deal damage to non-Fighting Pokemon
      const dealDamageToNonFighting = (p: any, playerType: PlayerType) => {
        p.forEachPokemon(playerType, (cardList: any) => {
          if (cardList.cards.length === 0) { return; }
          const checkType = new CheckPokemonTypeEffect(cardList);
          store.reduceEffect(state, checkType);
          if (!checkType.cardTypes.includes(CardType.FIGHTING)) {
            // Active gets DealDamageEffect (applies W/R), benched gets PutDamageEffect
            if (cardList === p.active) {
              const damageEffect = new DealDamageEffect(effect, 20);
              damageEffect.target = cardList;
              store.reduceEffect(state, damageEffect);
            } else {
              const damageEffect = new PutDamageEffect(effect, 20);
              damageEffect.target = cardList;
              store.reduceEffect(state, damageEffect);
            }
          }
        });
      };

      dealDamageToNonFighting(player, PlayerType.BOTTOM_PLAYER);
      dealDamageToNonFighting(opponent, PlayerType.TOP_PLAYER);
    }

    return state;
  }
}
