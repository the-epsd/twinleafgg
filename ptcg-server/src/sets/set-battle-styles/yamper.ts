// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SuperType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, GameMessage, ConfirmPrompt } from '../../game';
import { TrainerCard } from '../../game/store/card/trainer-card';
import { Effect } from '../../game/store/effects/effect';
import { PlayPokemonEffect } from '../../game/store/effects/play-card-effects';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Yamper extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = L;
  public hp: number = 60;
  public weakness = [{ type: F }];
  public retreat = [C];

  public powers = [{
    name: 'Ball Search',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand onto your Bench during your turn, you may put a Poké Ball card, a Great Ball card, or 1 of each from your discard pile into your hand.'
  }];

  public attacks = [
    {
      name: 'Flop',
      cost: [C, C],
      damage: 20,
      text: ''
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'BST';
  public setNumber: string = '52';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Yamper';
  public fullName: string = 'Yamper BST';

  // Ref: set-steam-siege/foongus.ts (Play Ball - PlayPokemonEffect trigger for ball retrieval ability)
  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    if (effect instanceof PlayPokemonEffect && effect.pokemonCard === this) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      // Find Poke Ball and Great Ball in discard
      const pokeBall = player.discard.cards.find(c =>
        c instanceof TrainerCard && c.superType === SuperType.TRAINER && c.name === 'Poké Ball'
      );
      const greatBall = player.discard.cards.find(c =>
        c instanceof TrainerCard && c.superType === SuperType.TRAINER && c.name === 'Great Ball'
      );

      if (!pokeBall && !greatBall) {
        return state;
      }

      state = store.prompt(state, new ConfirmPrompt(
        player.id,
        GameMessage.WANT_TO_USE_ABILITY
      ), wantToUse => {
        if (!wantToUse) {
          return;
        }

        if (pokeBall) {
          player.discard.moveCardTo(pokeBall, player.hand);
        }
        if (greatBall) {
          player.discard.moveCardTo(greatBall, player.hand);
        }
      });
    }

    return state;
  }
}
