// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PutCountersEffect } from '../../game/store/effects/attack-effects';
import { WAS_ATTACK_USED, ADD_MARKER, ON_DAMAGED_BY_OPPONENT_ATTACK_EVEN_IF_KNOCKED_OUT, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class DracozoltVmax extends PokemonCard {
  public tags = [CardTag.POKEMON_VMAX];
  public stage: Stage = Stage.VMAX;
  public evolvesFrom: string = 'Dracozolt V';
  public cardType: CardType = L;
  public hp: number = 330;
  public weakness = [{ type: F }];
  public retreat = [C, C, C];

  public readonly SPARK_TRAP_MARKER = 'DRACOZOLT_VMAX_EVS_SPARK_TRAP_MARKER';
  public readonly CLEAR_SPARK_TRAP_MARKER = 'DRACOZOLT_VMAX_EVS_CLEAR_SPARK_TRAP_MARKER';

  public attacks = [
    {
      name: 'Spark Trap',
      cost: [L],
      damage: 60,
      text: 'During your opponent\'s next turn, if this Pokémon is damaged by an attack (even if it is Knocked Out), put 12 damage counters on the Attacking Pokémon.'
    },
    {
      name: 'Max Impact',
      cost: [L, C, C, C],
      damage: 200,
      text: ''
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'EVS';
  public setNumber: string = '59';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Dracozolt VMAX';
  public fullName: string = 'Dracozolt VMAX EVS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Spark Trap
    // During your opponent's next turn, if this Pokémon is damaged by an attack
    // (even if it is Knocked Out), put 12 damage counters on the Attacking Pokémon.
    // Ref: set-unbroken-bonds/aggron.ts (Extra-Tight Press - ON_DAMAGED_BY_OPPONENT_ATTACK_EVEN_IF_KNOCKED_OUT)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      ADD_MARKER(this.SPARK_TRAP_MARKER, player.active, this);
      ADD_MARKER(this.CLEAR_SPARK_TRAP_MARKER, opponent, this);
    }

    // When damaged during opponent's next turn, put 12 damage counters on attacker
    if (ON_DAMAGED_BY_OPPONENT_ATTACK_EVEN_IF_KNOCKED_OUT(state, effect, { source: this })) {
      if (effect.target.marker.hasMarker(this.SPARK_TRAP_MARKER, this)) {
        const damageEffect = new PutCountersEffect(effect, 120); // 12 damage counters = 120 damage
        damageEffect.target = effect.source;
        store.reduceEffect(state, damageEffect);
      }
    }

    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_SPARK_TRAP_MARKER, this.SPARK_TRAP_MARKER, this);

    return state;
  }
}
