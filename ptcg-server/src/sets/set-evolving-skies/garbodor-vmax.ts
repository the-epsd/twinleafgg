// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, CardTag } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, BLOCK_RETREAT, BLOCK_RETREAT_IF_MARKER, REMOVE_MARKER_FROM_ACTIVE_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_POISIONED } from '../../game/store/prefabs/attack-effects';
import { MarkerConstants } from '../../game/store/markers/marker-constants';

export class GarbodorVmax extends PokemonCard {
  public tags = [CardTag.POKEMON_VMAX];
  public stage: Stage = Stage.VMAX;
  public evolvesFrom: string = 'Garbodor V';
  public cardType: CardType = D;
  public hp: number = 330;
  public weakness = [{ type: F }];
  public retreat = [C, C, C];

  // Rubbish Collecting: This Pokémon may have up to 2 Pokémon Tools attached to it.
  // Setting maxTools = 2 allows the system to accept the second tool attachment.
  // Ref: set-evolving-skies/play-trainer-effect.ts (tools.length >= pokemonCard.maxTools check)
  public maxTools: number = 2;

  public powers = [{
    name: 'Rubbish Collecting',
    powerType: PowerType.ABILITY,
    text: 'This Pokémon may have up to 2 Pokémon Tools attached to it. If it loses this Ability, discard Pokémon Tools from it until only 1 remains.'
  }];

  public attacks = [
    {
      name: 'G-Max Malodor',
      cost: [D, C],
      damage: 120,
      text: 'Your opponent\'s Active Pokémon is now Poisoned. During your opponent\'s next turn, that Pokémon can\'t retreat.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'EVS';
  public setNumber: string = '101';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Garbodor VMAX';
  public fullName: string = 'Garbodor VMAX EVS';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Rubbish Collecting
    // This Pokémon may have up to 2 Pokémon Tools attached to it.
    // Implemented via maxTools = 2 above - the tool attachment system uses this.
    // The "If it loses this Ability, discard Pokémon Tools until only 1 remains" behavior
    // is complex and would need IS_ABILITY_BLOCKED check on each AttachPokemonToolEffect.

    // Attack 1: G-Max Malodor
    // Your opponent's Active Pokémon is now Poisoned. During your opponent's next turn, that Pokémon can't retreat.
    // Ref: set-evolving-skies/tentacruel.ts (same pattern - Poisonous Prison)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_POISIONED(store, state, effect);
      return BLOCK_RETREAT(store, state, effect, this);
    }

    BLOCK_RETREAT_IF_MARKER(effect, MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);
    REMOVE_MARKER_FROM_ACTIVE_AT_END_OF_TURN(effect, MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);

    return state;
  }
}
