// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, BoardEffect, SpecialCondition } from '../../game/store/card/card-types';
import { PlayerType, PowerType, StoreLike, State, StateUtils, GameError, GameMessage, PokemonCardList } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AttackEffect, PowerEffect } from '../../game/store/effects/game-effects';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { EnergyCard } from '../../game/store/card/energy-card';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';
import { DISCARD_X_ENERGY_FROM_THIS_POKEMON } from '../../game/store/prefabs/costs';

export class Charizard extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Charmeleon';
  public cardType: CardType = R;
  public hp: number = 120;
  public weakness = [{ type: W }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C, C, C];

  public powers = [{
    name: 'Energy Burn',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'As often as you like during your turn (before your attack), you may turn all Energy attached to Charizard into [R] Energy for the rest of the turn. This power can\'t be used if Charizard is Asleep, Confused, or Paralyzed.'
  }];

  public attacks = [
    {
      name: 'Fire Spin',
      cost: [R, R, R, R],
      damage: 100,
      text: 'Discard 2 Energy cards attached to Charizard in order to use this attack.'
    }
  ];

  public set: string = 'CEL';
  public setNumber: string = '4A';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Charizard';
  public fullName: string = 'Charizard CEL';

  public readonly ENERGY_BURN_MARKER = 'CHARIZARD_CEL_ENERGY_BURN_MARKER';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Energy Burn
    // Ref: set-base-set/charizard.ts (Energy Burn - same power text, same implementation)
    if (effect instanceof PowerEffect && effect.power === this.powers[0]) {
      const player = effect.player;
      const cardList = StateUtils.findCardList(state, this) as PokemonCardList;

      if (cardList.specialConditions.includes(SpecialCondition.ASLEEP) ||
        cardList.specialConditions.includes(SpecialCondition.CONFUSED) ||
        cardList.specialConditions.includes(SpecialCondition.PARALYZED)) {
        return state;
      }

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      if (player.marker.hasMarker(this.ENERGY_BURN_MARKER, this)) {
        throw new GameError(GameMessage.POWER_ALREADY_USED);
      }

      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (pCardList) => {
        if (pCardList.getPokemonCard() === this) {
          pCardList.addBoardEffect(BoardEffect.ABILITY_USED);
        }
      });

      player.marker.addMarker(this.ENERGY_BURN_MARKER, this);
    }

    // Convert all energy to Fire when marker is set
    if (effect instanceof CheckProvidedEnergyEffect && effect.source.cards.includes(this)
      && effect.player.marker.hasMarker(this.ENERGY_BURN_MARKER, this)) {
      effect.source.cards.forEach(c => {
        if (c instanceof EnergyCard && !effect.energyMap.some(e => e.card === c)) {
          effect.energyMap.push({ card: c, provides: [CardType.FIRE] });
        }
      });
    }

    // Also update attack cost when energy burn is active
    if (effect instanceof AttackEffect && effect.player.marker.hasMarker(this.ENERGY_BURN_MARKER, this)) {
      effect.attack.cost = effect.attack.cost.map(() => CardType.FIRE);
    }

    // Attack 1: Fire Spin
    // Ref: set-base-set/charizard.ts (Fire Spin - discard 2 energy)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      DISCARD_X_ENERGY_FROM_THIS_POKEMON(store, state, effect, 2);
    }

    // Clean up marker at end of turn
    if (effect instanceof EndTurnEffect && effect.player.marker.hasMarker(this.ENERGY_BURN_MARKER, this)) {
      effect.player.marker.removeMarker(this.ENERGY_BURN_MARKER, this);
    }

    return state;
  }
}
