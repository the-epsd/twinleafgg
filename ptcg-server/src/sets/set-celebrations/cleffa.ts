// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, MOVE_CARDS, SHUFFLE_DECK, DRAW_CARDS } from '../../game/store/prefabs/prefabs';

export class Cleffa extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 30;
  public retreat = [];

  public attacks = [
    {
      name: 'Eeeeeeek',
      cost: [C],
      damage: 0,
      text: 'Shuffle your hand into your deck, then draw 7 cards.'
    }
  ];

  public set: string = 'CEL';
  public setNumber: string = '20A';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Cleffa';
  public fullName: string = 'Cleffa CEL';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Eeeeeeek
    // Ref: set-neo-genesis/cleffa.ts (Eeeeeeek)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      MOVE_CARDS(store, state, player.hand, player.deck);
      SHUFFLE_DECK(store, state, player);
      DRAW_CARDS(player, 7);
    }

    return state;
  }
}
