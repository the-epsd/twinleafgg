// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { ADD_SLEEP_TO_PLAYER_ACTIVE, AFTER_ATTACK, COIN_FLIP_PROMPT, SEARCH_DECK_FOR_CARDS_TO_HAND, WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';
import { CardType, Stage } from '../../game/store/card/card-types';
import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Effect } from '../../game/store/effects/effect';
import { State, StoreLike } from '../../game';
export class Delibird extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = W;
  public hp: number = 80;
  public weakness = [{ type: M }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Present',
      cost: [C],
      damage: 0,
      text: 'Flip a coin. If heads, search your deck for a card and put it into your hand. Shuffle your deck afterward.'
    },
    {
      name: 'Icy Wind',
      cost: [W, C],
      damage: 30,
      text: 'The Defending Pokémon is now Asleep.'
    }
  ];

  public set: string = 'BCR';
  public setNumber: string = '38';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Delibird';
  public fullName: string = 'Delibird BCR';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Present
    // Refs: set-dark-explorers/scrafty.ts (coin handling), set-boundaries-crossed/skyla.ts (search deck to hand)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      return COIN_FLIP_PROMPT(store, state, effect.player, result => {
        if (result) {
          SEARCH_DECK_FOR_CARDS_TO_HAND(
            store,
            state,
            effect.player,
            this,
            {},
            { min: 1, max: 1, allowCancel: false },
            this.attacks[0]
          );
        }
      });
    }

    // Attack 2: Icy Wind
    // Ref: set-noble-victories/cryogonal.ts (Frozen Whirlpool)
    if (AFTER_ATTACK(effect, 1, this)) {
      ADD_SLEEP_TO_PLAYER_ACTIVE(store, state, effect.opponent, this);
    }

    return state;
  }
}
