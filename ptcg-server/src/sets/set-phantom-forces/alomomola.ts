// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AfterAttackEffect, EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, SWITCH_ACTIVE_WITH_BENCHED } from '../../game/store/prefabs/prefabs';
import { HEAL_X_DAMAGE_FROM_THIS_POKEMON } from '../../game/store/prefabs/attack-effects';

export class Alomomola extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = W;
  public hp: number = 100;
  public weakness = [{ type: G }];
  public retreat = [C, C, C];

  public attacks = [
    {
      name: 'Super Deep Dive',
      cost: [W],
      damage: 0,
      text: 'Heal 30 damage from this Pokémon. Switch this Pokémon with 1 of your Benched Pokémon.'
    },
    {
      name: 'Surf',
      cost: [W, C, C, C],
      damage: 80,
      text: ''
    }
  ];

  public set: string = 'PHF';
  public setNumber: string = '22';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Alomomola';
  public fullName: string = 'Alomomola PHF';

  public usedSuperDeepDive = false;

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Super Deep Dive
    // Refs: set-furious-fists/breloom.ts (heal), AGENTS-patterns.md (post-damage switch self)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      HEAL_X_DAMAGE_FROM_THIS_POKEMON(30, effect, store, state);
      this.usedSuperDeepDive = true;
    }

    if (effect instanceof AfterAttackEffect && this.usedSuperDeepDive) {
      this.usedSuperDeepDive = false;
      const player = effect.player;
      if (player.bench.some(b => b.cards.length > 0)) {
        SWITCH_ACTIVE_WITH_BENCHED(store, state, player);
      }
    }

    if (effect instanceof EndTurnEffect && this.usedSuperDeepDive) {
      this.usedSuperDeepDive = false;
    }

    return state;
  }
}
