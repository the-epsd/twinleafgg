// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, NEXT_TURN_ATTACK_BONUS } from '../../game/store/prefabs/prefabs';
import { DISCARD_X_ENERGY_FROM_THIS_POKEMON } from '../../game/store/prefabs/costs';

export class Torkoal extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = R;
  public hp: number = 110;
  public weakness = [{ type: W }];
  public retreat = [C, C, C];

  public readonly HIGH_PRESSURE_HEAT_MARKER = 'TORKOAL_HIGH_PRESSURE_HEAT_MARKER';
  public readonly CLEAR_HIGH_PRESSURE_HEAT_MARKER = 'TORKOAL_CLEAR_HIGH_PRESSURE_HEAT_MARKER';

  public attacks = [
    {
      name: 'High-Pressure Heat',
      cost: [C, C],
      damage: 30,
      text: 'During your next turn, this Pokémon\'s High-Pressure Heat attack does 50 more damage (before applying Weakness and Resistance).'
    },
    {
      name: 'Flamethrower',
      cost: [R, R, C],
      damage: 90,
      text: 'Discard an Energy from this Pokémon.'
    }
  ];

  public set: string = 'SLG';
  public setNumber: string = '11';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Torkoal';
  public fullName: string = 'Torkoal SLG';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: High-Pressure Heat
    // Ref: set-guardians-rising/clefable.ts (Meteor Mash)
    NEXT_TURN_ATTACK_BONUS(effect, {
      attack: this.attacks[0],
      source: this,
      bonusDamage: 50,
      bonusMarker: this.HIGH_PRESSURE_HEAT_MARKER,
      clearMarker: this.CLEAR_HIGH_PRESSURE_HEAT_MARKER
    });

    // Attack 2: Flamethrower
    // Ref: set-burning-shadows/kingdra.ts (Tornado Shot - discard energy)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      DISCARD_X_ENERGY_FROM_THIS_POKEMON(store, state, effect, 1);
    }

    return state;
  }
}
