// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckRetreatCostEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';

export class Probopass extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Nosepass';
  public cardType: CardType = M;
  public hp: number = 140;
  public weakness = [{ type: R }];
  public resistance = [{ type: G, value: -30 }];
  public retreat = [C, C, C, C];

  public attacks = [
    {
      name: 'Gravitational Drop',
      cost: [M],
      damage: 10,
      damageCalculation: '+' as '+',
      text: 'This attack does 30 more damage for each Colorless in your opponent\'s Active Pokémon\'s Retreat Cost.'
    },
    {
      name: 'Heavy Impact',
      cost: [M, M, C],
      damage: 120,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'RCL';
  public setNumber: string = '131';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Probopass';
  public fullName: string = 'Probopass RCL';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Gravitational Drop
    // Ref: set-unbroken-bonds/beast-bringer.ts (CheckRetreatCostEffect - count retreat cost)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      const checkRetreatCost = new CheckRetreatCostEffect(opponent);
      store.reduceEffect(state, checkRetreatCost);
      const colorlessCount = checkRetreatCost.cost.filter(c => c === CardType.COLORLESS).length;
      effect.damage += 30 * colorlessCount;
    }

    return state;
  }
}
