// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, GameError, GameMessage, ShuffleDeckPrompt, PokemonCardList } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Starmie extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Staryu';
  public cardType: CardType = W;
  public hp: number = 90;
  public weakness = [{ type: G }];
  public retreat = [C];

  public readonly ESCAPE_MARKER = 'STARMIE_CIN_ESCAPE_MARKER';

  public powers = [{
    name: 'Escape',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may shuffle this Pokémon and all cards attached to it into your deck.'
  }];

  public attacks = [
    {
      name: 'Spinning Attack',
      cost: [C],
      damage: 40,
      text: ''
    }
  ];

  public set: string = 'CIN';
  public setNumber: string = '16';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Starmie';
  public fullName: string = 'Starmie CIN';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Escape
    // Ref: set-guardians-rising/politoed.ts (Hyper Jump - shuffle Pokemon + attached into deck)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.ESCAPE_MARKER, this);
      ABILITY_USED(player, this);

      // Find which card list this Pokemon is in
      const cardList = StateUtils.findCardList(state, this) as PokemonCardList;

      // Can't shuffle if it's the only Pokemon in play (active and no bench)
      if (cardList === player.active && !player.bench.some(b => b.cards.length > 0)) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      // Shuffle this Pokemon and all attached cards into deck
      cardList.moveTo(player.deck);
      cardList.clearEffects();

      return store.prompt(state, new ShuffleDeckPrompt(player.id), order => {
        player.deck.applyOrder(order);
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.ESCAPE_MARKER, this);

    return state;
  }
}
