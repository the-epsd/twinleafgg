// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, GameError, GameMessage, PokemonCardList } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, ABILITY_USED, USE_ABILITY_ONCE_PER_TURN, REMOVE_MARKER_AT_END_OF_TURN, COIN_FLIP_PROMPT } from '../../game/store/prefabs/prefabs';
import { StateUtils } from '../../game/store/state-utils';

export class Lopunny extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Buneary';
  public cardType: CardType = C;
  public hp: number = 90;
  public weakness = [{ type: F }];
  public retreat = [C];

  public readonly BIG_JUMP_MARKER = 'LOPUNNY_BIG_JUMP_MARKER';

  public powers = [{
    name: 'Big Jump',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may return this Pok\u00e9mon and all cards attached to it to your hand.'
  }];

  public attacks = [
    {
      name: 'Sitdown Bounce',
      cost: [C, C, C],
      damage: 80,
      text: 'Flip a coin. If tails, this Pok\u00e9mon can\'t attack during your next turn.'
    }
  ];

  public set: string = 'FLF';
  public setNumber: string = '85';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Lopunny';
  public fullName: string = 'Lopunny FLF';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Refs: set-legendary-treasures/phione.ts (Aqua Boomerang - return to hand), AGENTS.md (once-per-turn ability)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.BIG_JUMP_MARKER, this);
      ABILITY_USED(player, this);

      const cardList = StateUtils.findCardList(state, this) as PokemonCardList;
      cardList.moveTo(player.hand);
      cardList.clearEffects();
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.BIG_JUMP_MARKER, this);

    // Ref: AGENTS-patterns.md (can't attack next turn)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      COIN_FLIP_PROMPT(store, state, effect.player, result => {
        if (!result) {
          effect.player.active.cannotAttackNextTurnPending = true;
        }
      });
    }

    return state;
  }
}
