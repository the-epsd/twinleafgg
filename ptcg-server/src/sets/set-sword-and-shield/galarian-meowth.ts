// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { DealDamageEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, REMOVE_MARKER_AT_END_OF_TURN, REPLACE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class GalarianMeowth extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = M;
  public hp: number = 70;
  public weakness = [{ type: R }];
  public resistance = [{ type: G, value: -30 }];
  public retreat = [C];

  public readonly HONE_CLAWS_MARKER = 'GALARIAN_MEOWTH_SSH_HONE_CLAWS_MARKER';
  public readonly CLEAR_HONE_CLAWS_MARKER = 'GALARIAN_MEOWTH_SSH_CLEAR_HONE_CLAWS_MARKER';

  public attacks = [
    {
      name: 'Hone Claws',
      cost: [C],
      damage: 0,
      text: 'During your next turn, this Pokémon\'s Slash attack does 60 more damage (before applying Weakness and Resistance).'
    },
    {
      name: 'Slash',
      cost: [M, C],
      damage: 30,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'SSH';
  public setNumber: string = '127';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Galarian Meowth';
  public fullName: string = 'Galarian Meowth SSH';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Hone Claws
    // Ref: set-cosmic-eclipse/herdier.ts (Work Up - during next turn all attacks do more damage, manual DealDamageEffect bonus)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      player.marker.addMarker(this.HONE_CLAWS_MARKER, this);
    }

    // Apply Slash bonus during next turn when this Pokemon is the attacker
    if (effect instanceof DealDamageEffect && effect.source) {
      const player = effect.player;
      if ((player.marker.hasMarker(this.HONE_CLAWS_MARKER, this) || player.marker.hasMarker(this.CLEAR_HONE_CLAWS_MARKER, this))
        && effect.source.cards.includes(this)
        && effect.source.getPokemonCard() === this
        && effect.attack === this.attacks[1]) {
        effect.damage += 60;
      }
    }

    // 2-phase marker cleanup for "during your next turn"
    if (effect instanceof EndTurnEffect) {
      REMOVE_MARKER_AT_END_OF_TURN(effect, this.CLEAR_HONE_CLAWS_MARKER, this);
      REPLACE_MARKER_AT_END_OF_TURN(effect, this.HONE_CLAWS_MARKER, this.CLEAR_HONE_CLAWS_MARKER, this);
    }

    return state;
  }
}
