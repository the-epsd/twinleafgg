// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { ADD_BURN_TO_PLAYER_ACTIVE, AFTER_ATTACK, DISCARD_ALL_ENERGY_FROM_POKEMON, WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';
import { CardType, Stage } from '../../game/store/card/card-types';
import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Effect } from '../../game/store/effects/effect';
import { State, StoreLike } from '../../game';
export class Cinderace2 extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Raboot';
  public cardType: CardType = R;
  public hp: number = 170;
  public weakness = [{ type: W }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Pyro Ball',
      cost: [R],
      damage: 50,
      text: 'Your opponent\'s Active Pokémon is now Burned.'
    },
    {
      name: 'Burning Kick',
      cost: [R, R],
      damage: 160,
      text: 'Discard all Energy from this Pokémon.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'SSH';
  public setNumber: string = '35';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Cinderace';
  public fullName: string = 'Cinderace SSH 35';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Pyro Ball
    // Ref: set-plasma-blast/houndoom.ts (Blazing Claws - Burned status)
    if (AFTER_ATTACK(effect, 0, this)) {
      ADD_BURN_TO_PLAYER_ACTIVE(store, state, effect.opponent, this);
    }

    // Attack 2: Burning Kick
    // Ref: set-legends-awakened/mesprit-lv-x.ts (DISCARD_ALL_ENERGY_FROM_POKEMON)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      DISCARD_ALL_ENERGY_FROM_POKEMON(store, state, effect, this);
    }

    return state;
  }
}
