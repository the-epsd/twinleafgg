// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AfterAttackEffect, EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, SWITCH_ACTIVE_WITH_BENCHED } from '../../game/store/prefabs/prefabs';

export class GalarianLinoone extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Galarian Zigzagoon';
  public cardType: CardType = D;
  public hp: number = 100;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public usedNightSlash: boolean = false;

  public attacks = [
    {
      name: 'Night Slash',
      cost: [D],
      damage: 20,
      text: 'Switch this Pokémon with 1 of your Benched Pokémon.'
    },
    {
      name: 'Hammer In',
      cost: [D, C, C],
      damage: 70,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'SSH';
  public setNumber: string = '118';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Galarian Linoone';
  public fullName: string = 'Galarian Linoone SSH';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Night Slash
    // Ref: set-plasma-blast/houndour.ts (AfterAttackEffect + SWITCH_ACTIVE_WITH_BENCHED)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      this.usedNightSlash = true;
    }

    if (effect instanceof AfterAttackEffect && this.usedNightSlash) {
      this.usedNightSlash = false;
      const player = effect.player;
      const hasBench = player.bench.some(b => b.cards.length > 0);
      if (hasBench) {
        SWITCH_ACTIVE_WITH_BENCHED(store, state, player);
      }
    }

    if (effect instanceof EndTurnEffect) {
      this.usedNightSlash = false;
    }

    return state;
  }
}
