// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { TrainerCard } from '../../game/store/card/trainer-card';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, DRAW_CARDS, SHOW_CARDS_TO_PLAYER } from '../../game/store/prefabs/prefabs';

export class Polteageist extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Sinistea';
  public cardType: CardType = P;
  public hp: number = 60;
  public weakness = [{ type: D }];
  public resistance = [{ type: F, value: -30 }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Teatime',
      cost: [C],
      damage: 0,
      text: 'Each player draws 2 cards.'
    },
    {
      name: 'Poltergeist',
      cost: [P, C],
      damage: 50,
      damageCalculation: 'x' as 'x',
      text: 'Your opponent reveals their hand. This attack does 50 damage for each Trainer card you find there.'
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'SSH';
  public setNumber: string = '90';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Polteageist';
  public fullName: string = 'Polteageist SSH';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Teatime
    // Ref: AGENTS-patterns.md (DRAW_CARDS for each player)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      DRAW_CARDS(player, 2);
      DRAW_CARDS(opponent, 2);
    }

    // Attack 2: Poltergeist
    // Ref: set-flashfire/luxray.ts (reveal opponent's hand, count Trainer cards)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      SHOW_CARDS_TO_PLAYER(store, state, player, opponent.hand.cards);

      const trainerCount = opponent.hand.cards.filter(c => c instanceof TrainerCard).length;
      effect.damage = 50 * trainerCount;
    }

    return state;
  }
}
