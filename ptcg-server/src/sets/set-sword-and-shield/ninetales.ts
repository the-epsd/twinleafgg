// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, EnergyType, SuperType } from '../../game/store/card/card-types';
import { PlayerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ATTACH_ENERGY_PROMPT } from '../../game/store/prefabs/prefabs';
import { SlotType } from '../../game/store/actions/play-card-action';

export class Ninetales extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Vulpix';
  public cardType: CardType = R;
  public hp: number = 120;
  public weakness = [{ type: W }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Flame Cloak',
      cost: [R],
      damage: 30,
      text: 'Attach a [R] Energy card from your discard pile to this Pokémon.'
    },
    {
      name: 'Fire Mane',
      cost: [R, C],
      damage: 70,
      text: ''
    }
  ];

  public regulationMark: string = 'D';
  public set: string = 'SSH';
  public setNumber: string = '23';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ninetales';
  public fullName: string = 'Ninetales SSH';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Flame Cloak
    // Ref: set-sword-and-shield/cinderace-3.ts (ATTACH_ENERGY_PROMPT from discard to self only)
    // Using SlotType.ACTIVE only as destination since Ninetales is always the attacking (active) Pokemon
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      ATTACH_ENERGY_PROMPT(
        store, state, player,
        PlayerType.BOTTOM_PLAYER, SlotType.DISCARD,
        [SlotType.ACTIVE],
        { superType: SuperType.ENERGY, energyType: EnergyType.BASIC, name: 'Fire Energy' },
        { min: 0, max: 1, allowCancel: false }
      );
    }

    return state;
  }
}
