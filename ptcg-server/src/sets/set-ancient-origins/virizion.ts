// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SuperType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND, GET_PLAYER_PRIZES } from '../../game/store/prefabs/prefabs';

export class Virizion extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = G;
  public hp: number = 110;
  public weakness = [{ type: R }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Bail Out',
      cost: [G],
      damage: 0,
      text: 'Put 2 Pokémon from your discard pile into your hand.'
    },
    {
      name: 'Prize Count',
      cost: [G, G],
      damage: 40,
      damageCalculation: '+' as '+',
      text: 'If you have more Prize cards left than your opponent, this attack does 80 more damage.'
    }
  ];

  public set: string = 'AOR';
  public setNumber: string = '12';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Virizion';
  public fullName: string = 'Virizion AOR';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Bail Out
    // Ref: AGENTS-patterns.md (discard pile search for Pokemon - public zone, mandatory)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const pokemonInDiscard = player.discard.cards.filter(c => c instanceof PokemonCard).length;
      const count = Math.min(2, pokemonInDiscard);
      if (count > 0) {
        SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND(store, state, player, this,
          { superType: SuperType.POKEMON },
          { min: count, max: 2, allowCancel: false }
        );
      }
    }

    // Attack 2: Prize Count
    // Ref: AGENTS-patterns.md (prize count comparison)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      const playerPrizes = GET_PLAYER_PRIZES(player).length;
      const opponentPrizes = GET_PLAYER_PRIZES(opponent).length;

      if (playerPrizes > opponentPrizes) {
        effect.damage += 80;
      }
    }

    return state;
  }
}
