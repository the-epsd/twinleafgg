// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, DEVOLVE_POKEMON, COIN_FLIP_PROMPT } from '../../game/store/prefabs/prefabs';
import { DISCARD_AN_ENERGY_FROM_OPPONENTS_ACTIVE_POKEMON } from '../../game/store/prefabs/attack-effects';

export class Claydol extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Baltoy';
  public cardType: CardType = P;
  public hp: number = 100;
  public weakness = [{ type: P }];
  public retreat = [C, C];

  public attacks = [
    {
      name: 'Rewind',
      cost: [P],
      damage: 0,
      text: 'Devolve each of your opponent\'s evolved Pokémon and put the highest Stage Evolution card on it into your opponent\'s hand.'
    },
    {
      name: 'Hyper Beam',
      cost: [P, C],
      damage: 30,
      text: 'Flip a coin. If heads, discard an Energy attached to your opponent\'s Active Pokémon.'
    }
  ];

  public set: string = 'AOR';
  public setNumber: string = '33';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Claydol';
  public fullName: string = 'Claydol AOR';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Rewind
    // Ref: set-breakpoint/espeon-ex.ts (Miraculous Shine)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      // Devolve opponent's active if evolved
      if (opponent.active.getPokemons().length > 1) {
        DEVOLVE_POKEMON(store, state, opponent.active, opponent.hand);
      }

      // Devolve each opponent's benched evolved Pokemon
      opponent.bench.forEach(benchSpot => {
        if (benchSpot.cards.length > 0 && benchSpot.getPokemons().length > 1) {
          DEVOLVE_POKEMON(store, state, benchSpot, opponent.hand);
        }
      });
    }

    // Attack 2: Hyper Beam
    // Ref: AGENTS-patterns.md (Coin flip + discard opponent energy)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      COIN_FLIP_PROMPT(store, state, effect.player, result => {
        if (result) {
          DISCARD_AN_ENERGY_FROM_OPPONENTS_ACTIVE_POKEMON(store, state, effect);
        }
      });
    }

    return state;
  }
}
