// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { TrainerCard } from '../../game/store/card/trainer-card';
import { CardType, TrainerType } from '../../game/store/card/card-types';
import { PokemonCardList, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckPokemonTypeEffect } from '../../game/store/effects/check-effects';
import { PlayPokemonEffect } from '../../game/store/effects/play-card-effects';

export class ForestOfGiantPlants extends TrainerCard {
  public trainerType: TrainerType = TrainerType.STADIUM;
  public set: string = 'AOR';
  public setNumber: string = '74';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Forest of Giant Plants';
  public fullName: string = 'Forest of Giant Plants AOR';
  public text: string = 'Each player\'s Grass Pokémon can evolve during his or her first turn or the turn he or she plays those Pokémon.';

  // Ref: set-flashfire/caterpie.ts (Adaptive Evolution - allow evolution on turn played)
  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    if (effect instanceof PlayPokemonEffect) {
      const stadiumCard = StateUtils.getStadiumCard(state);
      if (stadiumCard !== this) {
        return state;
      }

      const player = effect.player;
      const pokemonCard = effect.pokemonCard;

      // Check if the Pokemon being played is a Grass type
      const checkType = new CheckPokemonTypeEffect(effect.target as PokemonCardList);
      store.reduceEffect(state, checkType);

      if (checkType.cardTypes.includes(CardType.GRASS) || pokemonCard.cardType === CardType.GRASS) {
        player.canEvolve = true;
        (effect.target as PokemonCardList).pokemonPlayedTurn = state.turn - 1;
      }
    }

    return state;
  }
}
