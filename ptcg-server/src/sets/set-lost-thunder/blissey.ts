// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { GameError, GameMessage, PowerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED, WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN, MULTIPLE_COIN_FLIPS_PROMPT } from '../../game/store/prefabs/prefabs';

export class Blissey extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Chansey';
  public cardType: CardType = C;
  public hp: number = 160;
  public weakness = [{ type: F }];
  public retreat = [C, C, C];

  public powers = [  {
    name: 'Happiness Supplement',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may remove a Special Condition from your Active Pokémon.'
  }];

  public attacks = [
    {
      name: 'Powerful Slap',
      cost: [C, C, C],
      damage: 80,
      damageCalculation: 'x' as 'x',
      text: 'Flip a coin for each Energy attached to this Pokémon. This attack does 80 damage for each heads.'
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '153';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Blissey';
  public fullName: string = 'Blissey LOT';

  public readonly HAPPINESS_MARKER = 'BLISSEY_LOT_HAPPINESS_MARKER';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Happiness Supplement
    // Ref: set-forbidden-light/azelf.ts (remove special conditions)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      if (player.active.specialConditions.length === 0) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.HAPPINESS_MARKER, this);
      ABILITY_USED(player, this);

      player.active.specialConditions = [];
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.HAPPINESS_MARKER, this);

    // Attack 1: Powerful Slap
    // Ref: AGENTS-patterns.md (multiple coin flips for damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const checkEnergy = new CheckProvidedEnergyEffect(player, player.active);
      store.reduceEffect(state, checkEnergy);
      const energyCount = checkEnergy.energyMap.length;

      MULTIPLE_COIN_FLIPS_PROMPT(store, state, player, energyCount, results => {
        const heads = results.filter(r => r).length;
        effect.damage = 80 * heads;
      });
    }

    return state;
  }
}
