// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, ConfirmPrompt, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, JUST_EVOLVED, MOVE_CARD_TO } from '../../game/store/prefabs/prefabs';
import { FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE } from '../../game/store/prefabs/attack-effects';

export class Ninjask extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Nincada';
  public cardType: CardType = G;
  public hp: number = 80;
  public weakness = [{ type: R }];
  public retreat = [];

  public powers = [  {
    name: 'Molting',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand to evolve 1 of your Pokémon during your turn, you may put a Shedinja from your discard pile onto your Bench.'
  }];

  public attacks = [
    {
      name: 'X-Scissor',
      cost: [G],
      damage: 40,
      damageCalculation: '+' as '+',
      text: 'Flip a coin. If heads, this attack does 40 more damage.'
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '30';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ninjask';
  public fullName: string = 'Ninjask LOT';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Molting (on-evolve - put Shedinja from discard onto bench)
    // Ref: set-forbidden-light/abomasnow.ts (Blessings of the Frost - JUST_EVOLVED)
    if (JUST_EVOLVED(effect, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const shedinjaInDiscard = player.discard.cards.find(c =>
        c instanceof PokemonCard && c.name === 'Shedinja'
      );

      const emptyBenchSlot = player.bench.find(b => b.cards.length === 0);

      if (!shedinjaInDiscard || !emptyBenchSlot) {
        return state;
      }

      store.prompt(state, new ConfirmPrompt(
        player.id,
        GameMessage.WANT_TO_USE_ABILITY
      ), wantToUse => {
        if (wantToUse) {
          MOVE_CARD_TO(state, shedinjaInDiscard, emptyBenchSlot);
          emptyBenchSlot.pokemonPlayedTurn = state.turn;
        }
      });
    }

    // Attack 1: X-Scissor
    // Ref: set-dragons-majesty/magikarp.ts (coin flip more damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE(store, state, effect, 40);
    }

    return state;
  }
}
