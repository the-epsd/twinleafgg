// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { EnergyCard } from '../../game/store/card/energy-card';
import { Effect } from '../../game/store/effects/effect';
import { CheckPokemonTypeEffect } from '../../game/store/effects/check-effects';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Kecleon2 extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 80;
  public weakness = [{ type: F }];
  public retreat = [C];

  public powers = [{
    name: 'Unit Color 3',
    powerType: PowerType.ABILITY,
    text: 'As long as this Pok\u00e9mon has Unit Energy FightingDarknessFairy attached to it, it is a Fighting, Darkness, and Fairy Pok\u00e9mon.'
  }];

  public attacks = [
    {
      name: 'Gentle Slap',
      cost: [C, C],
      damage: 60,
      text: ''
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '162';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Kecleon';
  public fullName: string = 'Kecleon LOT 162';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Unit Color 3 (passive - type change)
    // Ref: set-forbidden-light/zygarde.ts (Earthen Aura - passive ability pattern)
    if (effect instanceof CheckPokemonTypeEffect && effect.target.cards.includes(this) && effect.target.getPokemonCard() === this) {
      const player = StateUtils.findOwner(state, effect.target);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      // Check if Unit Energy FightingDarknessFairy is attached
      const hasUnitEnergy = effect.target.cards.some(c =>
        c instanceof EnergyCard && c.name === 'Unit Energy FightingDarknessFairy'
      );

      if (hasUnitEnergy) {
        if (!effect.cardTypes.includes(CardType.FIGHTING)) {
          effect.cardTypes.push(CardType.FIGHTING);
        }
        if (!effect.cardTypes.includes(CardType.DARK)) {
          effect.cardTypes.push(CardType.DARK);
        }
        if (!effect.cardTypes.includes(CardType.FAIRY)) {
          effect.cardTypes.push(CardType.FAIRY);
        }
      }
    }

    return state;
  }
}
