// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { CheckPokemonTypeEffect, CheckProvidedEnergyEffect } from '../../game/store/effects/check-effects';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Kecleon extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 80;
  public weakness = [{ type: F }];
  public retreat = [C];

  public powers = [{
    name: 'Unit Color 1',
    powerType: PowerType.ABILITY,
    text: 'As long as this Pokémon has Unit Energy GrassFireWater attached to it, it is a Grass, Fire, and Water Pokémon.'
  }];

  public attacks = [
    {
      name: 'Tongue Smack',
      cost: [C],
      damage: 10,
      damageCalculation: '+' as '+',
      text: 'If your opponent\'s Active Pokémon is an Evolution Pokémon, this attack does 50 more damage.'
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '161';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Kecleon';
  public fullName: string = 'Kecleon LOT';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Unit Color 1 (passive - type change when Unit Energy GFW attached)
    // Ref: set-celestial-storm/kecleon.ts (Unit Color 2 - CheckPokemonTypeEffect pattern)
    if (effect instanceof CheckPokemonTypeEffect && effect.target.getPokemonCard() === this) {
      const player = StateUtils.findOwner(state, effect.target);

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const checkEnergy = new CheckProvidedEnergyEffect(player, effect.target);
      store.reduceEffect(state, checkEnergy);

      const hasUnitEnergyGFW = checkEnergy.energyMap.some(em =>
        em.card.name === 'Unit Energy GRW'
      );

      if (hasUnitEnergyGFW) {
        if (!effect.cardTypes.includes(CardType.GRASS)) {
          effect.cardTypes.push(CardType.GRASS);
        }
        if (!effect.cardTypes.includes(CardType.FIRE)) {
          effect.cardTypes.push(CardType.FIRE);
        }
        if (!effect.cardTypes.includes(CardType.WATER)) {
          effect.cardTypes.push(CardType.WATER);
        }
      }
    }

    // Attack 1: Tongue Smack
    // Ref: set-dragons-majesty/kommo-o.ts (conditional damage)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      const opponentActive = opponent.active.getPokemonCard();
      if (opponentActive && opponentActive.stage !== Stage.BASIC) {
        effect.damage += 50;
      }
    }

    return state;
  }
}
