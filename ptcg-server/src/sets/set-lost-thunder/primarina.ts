// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, SuperType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, GameMessage, PlayerType, SlotType, AttachEnergyPrompt } from '../../game';
import { EnergyCard } from '../../game/store/card/energy-card';
import { Effect } from '../../game/store/effects/effect';
import { AttachEnergyEffect } from '../../game/store/effects/play-card-effects';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, ABILITY_USED, CONFIRMATION_PROMPT, ADD_MARKER, HAS_MARKER, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP } from '../../game/store/prefabs/attack-effects';

export class Primarina extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Brionne';
  public cardType: CardType = W;
  public hp: number = 150;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public readonly HARMONICS_MARKER = 'HARMONICS_MARKER';

  public powers = [{
    name: 'Harmonics',
    powerType: PowerType.ABILITY,
    text: 'Whenever you attach an Energy card from your hand to 1 of your Pokémon, except with an attack, Ability, or Trainer card, attach up to 2 Energy cards to that Pokémon instead of 1.'
  }];

  public attacks = [
    {
      name: 'Hypno Splash',
      cost: [W, C, C],
      damage: 80,
      text: 'Your opponent\'s Active Pokémon is now Asleep.'
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '67';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Primarina';
  public fullName: string = 'Primarina LOT';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Harmonics (passive - intercept manual energy attachment)
    // Ref: set-expedition/venusaur.ts (Harvest Bounty - attach extra energy on AttachEnergyEffect)
    if (effect instanceof AttachEnergyEffect) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      // Prevent infinite recursion - once per turn
      if (HAS_MARKER(this.HARMONICS_MARKER, player, this)) {
        return state;
      }

      // Check this Pokemon is in play
      let isInPlay = false;
      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card) => {
        if (card === this) {
          isInPlay = true;
        }
      });

      if (!isInPlay) {
        return state;
      }

      // Count energy cards in hand (excluding the one already being attached)
      const energyInHand = player.hand.cards.filter(
        c => c instanceof EnergyCard && c !== effect.energyCard
      );

      if (energyInHand.length > 0) {
        const attachTarget = effect.target;

        CONFIRMATION_PROMPT(store, state, player, result => {
          if (result) {
            ADD_MARKER(this.HARMONICS_MARKER, player, this);
            ABILITY_USED(player, this);

            // Allow attaching 1 more energy card to the SAME target
            store.prompt(state, new AttachEnergyPrompt(
              player.id,
              GameMessage.ATTACH_ENERGY_CARDS,
              player.hand,
              PlayerType.BOTTOM_PLAYER,
              [SlotType.ACTIVE, SlotType.BENCH],
              { superType: SuperType.ENERGY },
              { allowCancel: false, min: 1, max: 1 }
            ), transfers => {
              transfers = transfers || [];
              for (const transfer of transfers) {
                const energyCard = transfer.card as EnergyCard;
                const attachEnergyEffect = new AttachEnergyEffect(player, energyCard, attachTarget);
                store.reduceEffect(state, attachEnergyEffect);
              }
            });
          }
        });
      }
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.HARMONICS_MARKER, this);

    // Attack 1: Hypno Splash
    // Ref: set-celestial-storm/deoxys-2.ts (Asleep status)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP(store, state, effect);
    }

    return state;
  }
}
