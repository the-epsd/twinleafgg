// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils } from '../../game';
import { AbstractAttackEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ADD_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Carbink2 extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = Y;
  public hp: number = 90;
  public weakness = [{ type: M }];
  public resistance = [{ type: D, value: -20 }];
  public retreat = [C];

  public readonly WONDER_RAY_MARKER = 'CARBINK_LOT_143_WONDER_RAY_MARKER';
  public readonly CLEAR_WONDER_RAY_MARKER = 'CARBINK_LOT_143_CLEAR_WONDER_RAY_MARKER';

  public attacks = [
    {
      name: 'Wonder Ray',
      cost: [C, C],
      damage: 30,
      text: 'During your opponent\'s next turn, prevent all effects of attacks, including damage, done to this Pokémon by any Pokémon that has an Ability.'
    },
    {
      name: 'Power Gem',
      cost: [Y, C, C],
      damage: 60,
      text: ''
    }
  ];

  public set: string = 'LOT';
  public setNumber: string = '143';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Carbink';
  public fullName: string = 'Carbink LOT 143';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Wonder Ray
    // Ref: set-celestial-storm/scyther.ts (Agility - prevent all effects marker pattern)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      ADD_MARKER(this.WONDER_RAY_MARKER, player.active, this);
      ADD_MARKER(this.CLEAR_WONDER_RAY_MARKER, opponent, this);
    }

    // Prevent all effects of attacks from Pokemon that have an Ability
    if (effect instanceof AbstractAttackEffect
      && effect.target.marker.hasMarker(this.WONDER_RAY_MARKER, this)
      && effect.target.getPokemonCard() === this) {
      const sourceCard = effect.source.getPokemonCard();
      if (sourceCard && sourceCard.powers && sourceCard.powers.some(p => p.powerType === PowerType.ABILITY)) {
        effect.preventDefault = true;
        return state;
      }
    }

    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_WONDER_RAY_MARKER, this.WONDER_RAY_MARKER, this);

    return state;
  }
}
