// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, TrainerType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED } from '../../game/store/prefabs/prefabs';
import { TrainerCard } from '../../game/store/card/trainer-card';

export class Porygon2 extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Porygon';
  public cardType: CardType = C;
  public hp: number = 90;
  public weakness = [{ type: F }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Garbage Attack',
      cost: [C],
      damage: 20,
      damageCalculation: 'x' as 'x',
      text: 'This attack does 20 damage for each Pokémon Tool card in the Lost Zone (both yours and your opponent\'s).'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'LOR';
  public setNumber: string = '141';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Porygon2';
  public fullName: string = 'Porygon2 (LOR 141)';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Garbage Attack
    // Ref: set-lost-origin/rotom-v.ts (TrainerType.TOOL filter + lostzone.cards usage),
    //      set-lost-origin/sableye.ts (lostzone.cards.length check)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      const countToolsInLostZone = (cards: any[]) =>
        cards.filter(c => c instanceof TrainerCard && c.trainerType === TrainerType.TOOL).length;

      const toolCount = countToolsInLostZone(player.lostzone.cards) +
        countToolsInLostZone(opponent.lostzone.cards);

      effect.damage = 20 * toolCount;
    }

    return state;
  }
}
