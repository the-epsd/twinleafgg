// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { ConfirmPrompt, GameMessage, ShowCardsPrompt, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, SHUFFLE_DECK } from '../../game/store/prefabs/prefabs';

export class Aipom extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 60;
  public weakness = [{ type: F }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Mischievous Tail',
      cost: [C],
      damage: 0,
      text: 'Look at the top card of your opponent\'s deck. You may have your opponent shuffle their deck.'
    },
    {
      name: 'Scratch',
      cost: [C],
      damage: 10,
      text: ''
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'LOR';
  public setNumber: string = '144';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Aipom';
  public fullName: string = 'Aipom LOR 144';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Mischievous Tail
    // Ref: set-vivid-voltage/drone-rotom.ts (look at top card of deck), set-vivid-voltage/girafarig.ts (may have opponent shuffle deck)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (opponent.deck.cards.length === 0) {
        return state;
      }

      // Look at top card of opponent's deck
      const topCard = opponent.deck.cards[0];

      store.prompt(state, new ShowCardsPrompt(
        player.id,
        GameMessage.CARDS_SHOWED_BY_EFFECT,
        [topCard],
      ), () => {
        // After looking, ask if we want to have them shuffle
        store.prompt(state, new ConfirmPrompt(player.id, GameMessage.WANT_TO_USE_ABILITY), wantToShuffle => {
          if (wantToShuffle) {
            SHUFFLE_DECK(store, state, opponent);
          }
        });
      });
    }

    return state;
  }
}
