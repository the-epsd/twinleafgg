// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType } from '../../game/store/card/pokemon-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { KnockOutEffect } from '../../game/store/effects/game-effects';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, SEARCH_DECK_FOR_CARDS_TO_HAND } from '../../game/store/prefabs/prefabs';
import { GamePhase } from '../../game/store/state/state';

export class Spiritomb extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = D;
  public hp: number = 60;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public powers = [  {
    name: 'Cursed Message',
    powerType: PowerType.ABILITY,
    text: 'If this Pokémon is Knocked Out by damage from an attack from your opponent\'s Pokémon, search your deck for a card and put it into your hand. Then, shuffle your deck.'
  }];

  public attacks = [
    {
      name: 'Chain of Spirits',
      cost: [D, D],
      damage: 10,
      damageCalculation: '+' as '+',
      text: 'This attack does 60 more damage for each Spiritomb in your discard pile.'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'LOR';
  public setNumber: string = '117';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Spiritomb';
  public fullName: string = 'Spiritomb LOR 117';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Cursed Message (passive on KO)
    // Ref: set-fusion-strike/farewell-bell.ts (KnockOutEffect trigger with deck search)
    // Ref: set-lost-origin/komala.ts (KnockOutEffect during ATTACK phase check)
    if (effect instanceof KnockOutEffect && effect.target.cards.includes(this)) {
      if (state.phase !== GamePhase.ATTACK) {
        return state;
      }

      const pokemonCard = effect.target.getPokemonCard();
      if (pokemonCard !== this) {
        return state;
      }

      const owner = StateUtils.findOwner(state, effect.target);
      if (!owner) {
        return state;
      }

      if (IS_ABILITY_BLOCKED(store, state, owner, this)) {
        return state;
      }

      if (owner.deck.cards.length > 0) {
        SEARCH_DECK_FOR_CARDS_TO_HAND(store, state, owner, this,
          {},
          { min: 1, max: 1, allowCancel: false }
        );
      }
    }

    // Attack 1: Chain of Spirits
    // Ref: set-lost-origin/gengar.ts (count copies of card in discard for damage bonus)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;

      const spiritombInDiscard = player.discard.cards.filter(
        c => c instanceof PokemonCard && c.name === 'Spiritomb'
      ).length;

      effect.damage += 60 * spiritombInDiscard;
    }

    return state;
  }
}
