// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PlayItemEffect, PlaySupporterEffect } from '../../game/store/effects/play-card-effects';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, ADD_MARKER, HAS_MARKER, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';
import { HEAL_X_DAMAGE_FROM_THIS_POKEMON } from '../../game/store/prefabs/attack-effects';

export class Vileplume extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Gloom';
  public cardType: CardType = G;
  public hp: number = 150;
  public weakness = [{ type: R }];
  public retreat = [C, C];

  public readonly BLOCK_SUPPORTER_MARKER = 'VILEPLUME_LOR_BLOCK_SUPPORTER_MARKER';
  public readonly BLOCK_ITEM_MARKER = 'VILEPLUME_LOR_BLOCK_ITEM_MARKER';

  public attacks = [
    {
      name: 'Mega Drain',
      cost: [G, C],
      damage: 50,
      text: 'Heal 30 damage from this Pokémon.'
    },
    {
      name: 'Allergy Storm',
      cost: [G, C, C],
      damage: 90,
      text: 'Flip a coin. If heads, during your opponent\'s next turn, they can\'t play any Supporter cards from their hand. If tails, during your opponent\'s next turn, they can\'t play any Item cards from their hand.'
    }
  ];

  public regulationMark: string = 'F';
  public set: string = 'LOR';
  public setNumber: string = '3';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Vileplume';
  public fullName: string = 'Vileplume LOR 3';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Mega Drain
    // Ref: set-celestial-storm/parasect.ts (HEAL_X_DAMAGE_FROM_THIS_POKEMON)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      HEAL_X_DAMAGE_FROM_THIS_POKEMON(30, effect, store, state);
    }

    // Attack 2: Allergy Storm
    // Ref: set-team-up/psyduck.ts (Headache - coin flip block trainers with markers)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          // Heads: block Supporter cards
          ADD_MARKER(this.BLOCK_SUPPORTER_MARKER, opponent, this);
        } else {
          // Tails: block Item cards
          ADD_MARKER(this.BLOCK_ITEM_MARKER, opponent, this);
        }
      });
    }

    if (effect instanceof PlaySupporterEffect) {
      const player = effect.player;
      if (HAS_MARKER(this.BLOCK_SUPPORTER_MARKER, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }
    }

    if (effect instanceof PlayItemEffect) {
      const player = effect.player;
      if (HAS_MARKER(this.BLOCK_ITEM_MARKER, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.BLOCK_SUPPORTER_MARKER, this);
    REMOVE_MARKER_AT_END_OF_TURN(effect, this.BLOCK_ITEM_MARKER, this);

    return state;
  }
}
