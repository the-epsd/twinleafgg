// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, StateUtils, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { ConfirmPrompt } from '../../game/store/prompts/confirm-prompt';
import { IS_ABILITY_BLOCKED, JUST_EVOLVED, COIN_FLIP_PROMPT, ADD_PARALYZED_TO_PLAYER_ACTIVE } from '../../game/store/prefabs/prefabs';

export class Servine extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Snivy';
  public cardType: CardType = G;
  public hp: number = 70;
  public weakness = [{ type: R }];
  public retreat = [C];

  public powers = [{
    name: 'Serpentine Strangle',
    powerType: PowerType.ABILITY,
    text: 'When you play this Pokémon from your hand to evolve 1 of your Pokémon, you may flip a coin. If heads, your opponent\'s Active Pokémon is now Paralyzed.'
  }];

  public attacks = [
    {
      name: 'Vine Whip',
      cost: [G],
      damage: 20,
      text: ''
    }
  ];

  public set: string = 'FCO';
  public setNumber: string = '6';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Servine';
  public fullName: string = 'Servine FCO';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Serpentine Strangle
    // Ref: set-flashfire/forretress.ts (Thorn Tempest - on-evolve ability)
    if (JUST_EVOLVED(effect, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const opponent = StateUtils.getOpponent(state, player);

      store.prompt(state, new ConfirmPrompt(
        player.id,
        GameMessage.WANT_TO_USE_ABILITY
      ), result => {
        if (!result) {
          return;
        }

        COIN_FLIP_PROMPT(store, state, player, flipResult => {
          if (flipResult) {
            ADD_PARALYZED_TO_PLAYER_ACTIVE(store, state, opponent, this);
          }
        });
      });
    }

    return state;
  }
}
