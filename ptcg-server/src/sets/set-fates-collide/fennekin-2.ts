// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AfterAttackEffect, EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, GUST_OPPONENT_BENCHED_POKEMON } from '../../game/store/prefabs/prefabs';

export class Fennekin2 extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = R;
  public hp: number = 50;
  public weakness = [{ type: W }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Invite Out',
      cost: [C],
      damage: 0,
      text: 'Flip a coin. If heads, switch 1 of your opponent\'s Benched Pokémon with his or her Active Pokémon.'
    }
  ];

  public set: string = 'FCO';
  public setNumber: string = '11';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Fennekin';
  public fullName: string = 'Fennekin FCO 11';

  public usedInviteOut = false;

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Invite Out
    // Refs: set-ancient-origins/malamar.ts (gust after attack), AGENTS-patterns.md (coin flip)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (opponent.bench.some(b => b.cards.length > 0)) {
        COIN_FLIP_PROMPT(store, state, player, result => {
          if (result) {
            this.usedInviteOut = true;
          }
        });
      }
    }

    if (effect instanceof AfterAttackEffect && this.usedInviteOut) {
      this.usedInviteOut = false;
      const player = effect.player;
      GUST_OPPONENT_BENCHED_POKEMON(store, state, player);
    }

    if (effect instanceof EndTurnEffect && this.usedInviteOut) {
      this.usedInviteOut = false;
    }

    return state;
  }
}
