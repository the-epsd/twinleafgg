// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { ChoosePokemonPrompt, GameError, GameMessage, PlayerType, PowerType, SlotType, StoreLike, State, PokemonCardList } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { HealEffect } from '../../game/store/effects/game-effects';
import { PlaceDamageCountersEffect } from '../../game/store/effects/game-effects';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN, COIN_FLIP_PROMPT } from '../../game/store/prefabs/prefabs';

export class GalarianSlowking extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Galarian Slowpoke';
  public cardType: CardType = D;
  public hp: number = 120;
  public weakness = [{ type: F }];
  public retreat = [C, C, C];

  public readonly MYSTERIOUS_POTION_MARKER = 'GALARIAN_SLOWKING_CRE_MYSTERIOUS_POTION_MARKER';

  public powers = [{
    name: 'Mysterious Potion',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn, you may choose 1 of your Pokémon and flip a coin. If heads, heal 90 damage from that Pokémon. If tails, put 3 damage counters on that Pokémon.'
  }];

  public attacks = [
    {
      name: 'Spray Fluid',
      cost: [D, C, C],
      damage: 90,
      text: ''
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'CRE';
  public setNumber: string = '98';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Galarian Slowking';
  public fullName: string = 'Galarian Slowking CRE';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Mysterious Potion
    // Ref: set-chilling-reign/siebold.ts (ChoosePokemonPrompt + HealEffect),
    //      set-vivid-voltage/eelektross.ts (PlaceDamageCountersEffect)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.MYSTERIOUS_POTION_MARKER, this);
      ABILITY_USED(player, this);

      return store.prompt(state, new ChoosePokemonPrompt(
        player.id,
        GameMessage.CHOOSE_POKEMON_TO_HEAL,
        PlayerType.BOTTOM_PLAYER,
        [SlotType.ACTIVE, SlotType.BENCH],
        { allowCancel: false, min: 1, max: 1 }
      ), (selected: PokemonCardList[] | null) => {
        const targets: PokemonCardList[] = selected || [];
        if (targets.length === 0) return;

        const target = targets[0];
        COIN_FLIP_PROMPT(store, state, player, (heads: boolean) => {
          if (heads) {
            const healEffect = new HealEffect(player, target, 90);
            store.reduceEffect(state, healEffect);
          } else {
            const placeCounters = new PlaceDamageCountersEffect(player, target, 30);
            store.reduceEffect(state, placeCounters);
          }
        });
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.MYSTERIOUS_POTION_MARKER, this);

    return state;
  }
}
