// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType, TrainerType } from '../../game/store/card/card-types';
import { PowerType, StoreLike, State, TrainerCard } from '../../game';
import { CheckAttackCostEffect } from '../../game/store/effects/check-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, DRAW_CARDS_UNTIL_CARDS_IN_HAND, IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Castform extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 70;
  public weakness = [{ type: F }];
  public retreat = [];

  public powers = [  {
    name: 'Weather Reading',
    powerType: PowerType.ABILITY,
    text: 'If you have 8 or more Stadium cards in your discard pile, ignore all Energy in this Pokémon\'s attack costs.'
  }];

  public attacks = [
    {
      name: 'Weather Force',
      cost: [C, C, C],
      damage: 80,
      text: 'Draw cards until you have 6 cards in your hand.'
    }
  ];

  public regulationMark: string = 'E';
  public set: string = 'CRE';
  public setNumber: string = '121';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Castform';
  public fullName: string = 'Castform CRE';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Weather Reading (passive CheckAttackCostEffect - if 8+ Stadiums in discard, ignore all energy in attack cost)
    // Ref: set-chilling-reign/castform-rainy-form.ts (exact same ability pattern)
    if (effect instanceof CheckAttackCostEffect && effect.attack === this.attacks[0]) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        return state;
      }

      const stadiumsInDiscard = player.discard.cards.filter(
        c => c instanceof TrainerCard && (c as TrainerCard).trainerType === TrainerType.STADIUM
      ).length;

      if (stadiumsInDiscard >= 8) {
        // Remove all energy from attack cost
        effect.cost.splice(0, effect.cost.length);
      }
    }

    // Attack 1: Weather Force
    // Ref: AGENTS-patterns.md (DRAW_CARDS_UNTIL_CARDS_IN_HAND)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      DRAW_CARDS_UNTIL_CARDS_IN_HAND(player, 6);
    }

    return state;
  }
}
