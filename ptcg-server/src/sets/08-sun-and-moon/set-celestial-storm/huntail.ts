// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../../game';
import { Effect } from '../../../game/store/effects/effect';
import { WAS_ATTACK_USED, BLOCK_RETREAT } from '../../../game/store/prefabs/prefabs';
export class Huntail extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Clamperl';
  public cardType: CardType = W;
  public hp: number = 110;
  public weakness = [{ type: G }];
  public retreat = [C];

  public attacks = [
    {
      name: 'Big Bite',
      cost: [W],
      damage: 30,
      text: 'The Defending Pokémon can\'t retreat during your opponent\'s next turn.'
    },
    {
      name: 'Dangerous Bite',
      cost: [W, C, C],
      damage: 40,
      damageCalculation: '+',
      text: 'If your opponent\'s Active Pokémon is a Basic Pokémon, this attack does 80 more damage.'
    }
  ];

  public set: string = 'CES';
  public setNumber: string = '42';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Huntail';
  public fullName: string = 'Huntail CES';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Big Bite
    // Ref: set-forbidden-light/dewpider.ts (Spider Web - BLOCK_RETREAT 3-call pattern)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      return BLOCK_RETREAT(store, state, effect, this);
    }

    // Attack 2: Dangerous Bite
    // Ref: AGENTS-patterns.md (conditional more damage)
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      const opponentCard = opponent.active.getPokemonCard();
      if (opponentCard && opponentCard.stage === Stage.BASIC) {
        effect.damage += 80;
      }
    }
    return state;
  }
}
