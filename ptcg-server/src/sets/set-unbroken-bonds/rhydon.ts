// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { TrainerEffect } from '../../game/store/effects/play-card-effects';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, DISCARD_TOP_X_CARDS_FROM_YOUR_DECK } from '../../game/store/prefabs/prefabs';

export class Rhydon extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Rhyhorn';
  public cardType: CardType = F;
  public hp: number = 110;
  public weakness = [{ type: G }];
  public retreat = [C, C, C, C];

  public attacks = [
    {
      name: 'Dirty Work',
      cost: [C, C],
      damage: 0,
      text: 'Discard the top card of your opponent\'s deck. If you played Giovanni\'s Exile from your hand during this turn, discard the top 5 cards instead.'
    },
    {
      name: 'Horn Attack',
      cost: [F, C, C, C],
      damage: 90,
      text: ''
    }
  ];

  public set: string = 'UNB';
  public setNumber: string = '94';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Rhydon';
  public fullName: string = 'Rhydon UNB';

  private readonly GIOVANNIS_EXILE_MARKER = 'RHYDON_GIOVANNIS_EXILE_MARKER';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Track if Giovanni's Exile was played this turn
    // Ref: set-ultra-prism/garchomp.ts (Royal Blades - tracking supporter played this turn)
    if (effect instanceof TrainerEffect && effect.trainerCard.name === 'Giovanni\'s Exile') {
      const player = effect.player;
      ADD_MARKER(this.GIOVANNIS_EXILE_MARKER, player, this);
    }

    if (effect instanceof EndTurnEffect && effect.player.marker.hasMarker(this.GIOVANNIS_EXILE_MARKER, this)) {
      effect.player.marker.removeMarker(this.GIOVANNIS_EXILE_MARKER, this);
    }

    // Attack 1: Dirty Work
    // Ref: set-ultra-prism/garchomp.ts (Royal Blades - supporter check), set-lost-thunder/heatran.ts (Heat Bazooka - discard from deck)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (HAS_MARKER(this.GIOVANNIS_EXILE_MARKER, player, this)) {
        DISCARD_TOP_X_CARDS_FROM_YOUR_DECK(store, state, opponent, 5, this, effect);
      } else {
        DISCARD_TOP_X_CARDS_FROM_YOUR_DECK(store, state, opponent, 1, this, effect);
      }
    }

    return state;
  }
}
