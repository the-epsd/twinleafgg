// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { DealDamageEffect } from '../../game/store/effects/attack-effects';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, COIN_FLIP_PROMPT, ADD_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Frogadier extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Froakie';
  public cardType: CardType = W;
  public hp: number = 80;
  public weakness = [{ type: G }];
  public retreat = [C];

  public readonly AFTERIMAGE_MARKER = 'FROGADIER_UNB_AFTERIMAGE_MARKER';
  public readonly CLEAR_AFTERIMAGE_MARKER = 'FROGADIER_UNB_CLEAR_AFTERIMAGE_MARKER';

  public attacks = [
    {
      name: 'Afterimage Strike',
      cost: [C],
      damage: 20,
      text: 'If any damage is done to this Pokémon by attacks during your opponent\'s next turn, flip a coin. If heads, prevent that damage.'
    }
  ];

  public set: string = 'UNB';
  public setNumber: string = '52';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Frogadier';
  public fullName: string = 'Frogadier UNB';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Afterimage Strike
    // Ref: set-guardians-rising/alolan-sandshrew.ts (Defense Curl - damage prevention with coin flip)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      ADD_MARKER(this.AFTERIMAGE_MARKER, player.active, this);
      ADD_MARKER(this.CLEAR_AFTERIMAGE_MARKER, opponent, this);
    }

    // Intercept damage - flip coin, if heads prevent damage
    if (effect instanceof DealDamageEffect
      && effect.target.marker.hasMarker(this.AFTERIMAGE_MARKER, this)
      && effect.target.getPokemonCard() === this) {
      const player = StateUtils.findOwner(state, effect.target);
      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          effect.damage = 0;
        }
      });
    }

    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_AFTERIMAGE_MARKER, this.AFTERIMAGE_MARKER, this);

    return state;
  }
}
