// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PlayerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AttackEffect } from '../../game/store/effects/game-effects';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, REMOVE_MARKER, DISCARD_TOP_X_CARDS_FROM_YOUR_DECK } from '../../game/store/prefabs/prefabs';

export class Haxorus extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Fraxure';
  public cardType: CardType = N;
  public hp: number = 160;
  public weakness = [{ type: Y }];
  public retreat = [C, C, C];

  public readonly DRAGON_DANCE_MARKER = 'HAXORUS_BKT_DRAGON_DANCE_MARKER';
  public readonly CLEAR_DRAGON_DANCE_MARKER = 'HAXORUS_BKT_CLEAR_DRAGON_DANCE_MARKER';

  public attacks = [
    {
      name: 'Dragon Dance',
      cost: [C],
      damage: 0,
      text: 'As long as this Haxorus is your Active Pokémon, each of its attacks does 100 more damage (before applying Weakness and Resistance). You can\'t add more than 100 damage in this way.'
    },
    {
      name: 'Sharp Fang',
      cost: [C],
      damage: 60,
      text: ''
    },
    {
      name: 'Dragon Pulse',
      cost: [F, M],
      damage: 130,
      text: 'Discard the top 3 cards of your deck.'
    }
  ];

  public set: string = 'BKT';
  public setNumber: string = '111';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Haxorus';
  public fullName: string = 'Haxorus BKT';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Check for Dragon Dance bonus on ANY attack from this Pokemon
    // Ref: set-roaring-skies/unfezant-2.ts (Feather Dance - next-turn all-attacks bonus)
    if (effect instanceof AttackEffect && effect.player.active.getPokemonCard() === this) {
      const player = effect.player;
      if (HAS_MARKER(this.DRAGON_DANCE_MARKER, player.active, this)) {
        effect.damage += 100;
      }
    }

    // Attack 1: Dragon Dance - set marker for next turn bonus
    // Ref: set-roaring-skies/unfezant-2.ts (Feather Dance)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      ADD_MARKER(this.DRAGON_DANCE_MARKER, effect.player.active, this);
    }

    // Attack 3: Dragon Pulse
    // Ref: set-roaring-skies/dunsparce.ts (Burrow - discard top cards)
    if (WAS_ATTACK_USED(effect, 2, this)) {
      DISCARD_TOP_X_CARDS_FROM_YOUR_DECK(store, state, effect.player, 3, this, effect);
    }

    // Two-marker pattern: marker persists through our next turn, then is cleared
    if (effect instanceof EndTurnEffect) {
      const player = effect.player;
      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList) => {
        if (cardList.getPokemonCard() === this) {
          if (HAS_MARKER(this.CLEAR_DRAGON_DANCE_MARKER, cardList, this)) {
            REMOVE_MARKER(this.DRAGON_DANCE_MARKER, cardList, this);
            REMOVE_MARKER(this.CLEAR_DRAGON_DANCE_MARKER, cardList, this);
          } else if (HAS_MARKER(this.DRAGON_DANCE_MARKER, cardList, this)) {
            ADD_MARKER(this.CLEAR_DRAGON_DANCE_MARKER, cardList, this);
          }
        }
      });
    }

    return state;
  }
}
