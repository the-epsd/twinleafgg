// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PlayerType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { AttackEffect } from '../../game/store/effects/game-effects';
import { EndTurnEffect } from '../../game/store/effects/game-phase-effects';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, REMOVE_MARKER } from '../../game/store/prefabs/prefabs';

export class Lillipup extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = C;
  public hp: number = 60;
  public weakness = [{ type: F }];
  public retreat = [C];

  public readonly WORK_UP_MARKER = 'LILLIPUP_SUM_WORK_UP';
  public readonly CLEAR_WORK_UP_MARKER = 'LILLIPUP_SUM_CLEAR_WORK_UP';

  public attacks = [
    {
      name: 'Work Up',
      cost: [C],
      damage: 0,
      text: 'During your next turn, this Pokémon\'s attacks do 20 more damage to your opponent\'s Active Pokémon (before applying Weakness and Resistance).'
    },
    {
      name: 'Bite',
      cost: [C, C],
      damage: 20,
      text: ''
    }
  ];

  public set: string = 'SUM';
  public setNumber: string = '103';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Lillipup';
  public fullName: string = 'Lillipup SUM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Check for bonus damage on ANY attack from this Pokemon
    // Ref: set-roaring-skies/unfezant-2.ts (Feather Dance)
    if (effect instanceof AttackEffect && effect.player.active.getPokemonCard() === this) {
      const player = effect.player;
      if (HAS_MARKER(this.WORK_UP_MARKER, player.active, this)) {
        effect.damage += 20;
      }
    }

    // Attack 1: Work Up - set marker for next turn bonus
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      ADD_MARKER(this.WORK_UP_MARKER, player.active, this);
    }

    // Two-marker pattern: marker persists through our next turn, then is cleared
    if (effect instanceof EndTurnEffect) {
      const player = effect.player;
      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList) => {
        if (cardList.getPokemonCard() === this) {
          if (HAS_MARKER(this.CLEAR_WORK_UP_MARKER, cardList, this)) {
            REMOVE_MARKER(this.WORK_UP_MARKER, cardList, this);
            REMOVE_MARKER(this.CLEAR_WORK_UP_MARKER, cardList, this);
          } else if (HAS_MARKER(this.WORK_UP_MARKER, cardList, this)) {
            ADD_MARKER(this.CLEAR_WORK_UP_MARKER, cardList, this);
          }
        }
      });
    }

    return state;
  }
}
