// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { DealDamageEffect } from '../../game/store/effects/attack-effects';
import { WAS_ATTACK_USED, ADD_MARKER, COIN_FLIP_PROMPT, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Cutiefly extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = Y;
  public hp: number = 30;
  public weakness = [{ type: M }];
  public resistance = [{ type: D, value: -20 }];
  public retreat = [C];

  public readonly FLY_AROUND_MARKER = 'CUTIEFLY_FLY_AROUND_MARKER';
  public readonly CLEAR_FLY_AROUND_MARKER = 'CUTIEFLY_CLEAR_FLY_AROUND_MARKER';

  public attacks = [
    {
      name: 'Fly Around',
      cost: [Y],
      damage: 10,
      text: 'If any damage is done to this Pokémon by attacks during your opponent\'s next turn, flip a coin. If heads, prevent that damage.'
    }
  ];

  public set: string = 'SUM';
  public setNumber: string = '92';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Cutiefly';
  public fullName: string = 'Cutiefly SUM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Fly Around
    // Ref: set-next-destinies/cinccino.ts (Smooth Coat - coin flip damage prevention)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);
      ADD_MARKER(this.FLY_AROUND_MARKER, player.active, this);
      ADD_MARKER(this.CLEAR_FLY_AROUND_MARKER, opponent, this);
    }

    // Intercept damage - flip coin to prevent
    if (effect instanceof DealDamageEffect && effect.target.marker.hasMarker(this.FLY_AROUND_MARKER, this)) {
      if (effect.damage > 0) {
        const player = StateUtils.findOwner(state, effect.target);
        return COIN_FLIP_PROMPT(store, state, player, result => {
          if (result) {
            effect.damage = 0;
          }
        });
      }
    }

    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_FLY_AROUND_MARKER, this.FLY_AROUND_MARKER, this);

    return state;
  }
}
