// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { CardTarget, GameError, GameMessage, PlayerType, PokemonCardList, PowerType, SlotType, StoreLike, State } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { HealEffect } from '../../game/store/effects/game-effects';
import { ChoosePokemonPrompt } from '../../game/store/prompts/choose-pokemon-prompt';
import { WAS_POWER_USED, IS_ABILITY_BLOCKED, USE_ABILITY_ONCE_PER_TURN, ABILITY_USED, REMOVE_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Ribombee extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Cutiefly';
  public cardType: CardType = Y;
  public hp: number = 60;
  public weakness = [{ type: M }];
  public resistance = [{ type: D, value: -20 }];
  public retreat = [C];

  public readonly CURATIVE_POLLEN_MARKER = 'RIBOMBEE_CURATIVE_POLLEN_MARKER';

  public powers = [{
    name: 'Curative Pollen',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may heal 20 damage from 1 of your Pokémon.'
  }];

  public attacks = [
    {
      name: 'Fairy Wind',
      cost: [Y],
      damage: 30,
      text: ''
    }
  ];

  public set: string = 'SUM';
  public setNumber: string = '93';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Ribombee';
  public fullName: string = 'Ribombee SUM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Curative Pollen
    // Refs: set-silver-tempest/radiant-tsareena.ts (Elegant Heal), set-x-and-y/spritzee.ts (Sweet Scent)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      const blocked: CardTarget[] = [];
      let hasPokemonWithDamage = false;
      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
        if (cardList.damage === 0) {
          blocked.push(target);
        } else {
          hasPokemonWithDamage = true;
        }
      });

      if (!hasPokemonWithDamage) {
        throw new GameError(GameMessage.CANNOT_USE_POWER);
      }

      USE_ABILITY_ONCE_PER_TURN(player, this.CURATIVE_POLLEN_MARKER, this);
      ABILITY_USED(player, this);

      let targets: PokemonCardList[] = [];
      return store.prompt(state, new ChoosePokemonPrompt(
        player.id,
        GameMessage.CHOOSE_POKEMON_TO_HEAL,
        PlayerType.BOTTOM_PLAYER,
        [SlotType.ACTIVE, SlotType.BENCH],
        { allowCancel: false, blocked }
      ), results => {
        targets = results || [];
        if (targets.length === 0) {
          return state;
        }

        targets.forEach(target => {
          const healEffect = new HealEffect(player, target, 20);
          store.reduceEffect(state, healEffect);
        });
        return state;
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.CURATIVE_POLLEN_MARKER, this);

    return state;
  }
}
