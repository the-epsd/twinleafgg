// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ADD_MARKER, COIN_FLIP_PROMPT, PREVENT_DAMAGE_IF_TARGET_HAS_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Metapod extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Caterpie';
  public cardType: CardType = G;
  public hp: number = 80;
  public weakness = [{ type: R }];
  public retreat = [C, C, C];

  public readonly IRON_DEFENSE_MARKER = 'METAPOD_IRON_DEFENSE_MARKER';
  public readonly CLEAR_IRON_DEFENSE_MARKER = 'METAPOD_CLEAR_IRON_DEFENSE_MARKER';

  public attacks = [
    {
      name: 'Iron Defense',
      cost: [C],
      damage: 0,
      text: 'Flip a coin. If heads, prevent all damage done to this Pokémon by attacks during your opponent\'s next turn.'
    },
    {
      name: 'Bug Bite',
      cost: [C, C, C],
      damage: 40,
      text: ''
    }
  ];

  public set: string = 'SUM';
  public setNumber: string = '2';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Metapod';
  public fullName: string = 'Metapod SUM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Iron Defense
    // Ref: set-mysterious-treasures/chansey.ts (Scrunch)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      return COIN_FLIP_PROMPT(store, state, player, result => {
        if (!result) {
          return;
        }
        ADD_MARKER(this.IRON_DEFENSE_MARKER, player.active, this);
        ADD_MARKER(this.CLEAR_IRON_DEFENSE_MARKER, opponent, this);
      });
    }

    PREVENT_DAMAGE_IF_TARGET_HAS_MARKER(effect, this.IRON_DEFENSE_MARKER, this);
    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_IRON_DEFENSE_MARKER, this.IRON_DEFENSE_MARKER, this);

    return state;
  }
}
