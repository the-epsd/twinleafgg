// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { EnergyCard, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { WAS_ATTACK_USED, ADD_MARKER, COIN_FLIP_PROMPT, PREVENT_DAMAGE_IF_TARGET_HAS_MARKER, CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN } from '../../game/store/prefabs/prefabs';

export class Togedemaru extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = L;
  public hp: number = 70;
  public weakness = [{ type: F }];
  public resistance = [{ type: M, value: -20 }];
  public retreat = [C];

  public readonly DEFENSE_CURL_MARKER = 'TOGEDEMARU_DEFENSE_CURL_MARKER';
  public readonly CLEAR_DEFENSE_CURL_MARKER = 'TOGEDEMARU_CLEAR_DEFENSE_CURL_MARKER';

  public attacks = [
    {
      name: 'Defense Curl',
      cost: [C],
      damage: 0,
      text: 'Flip a coin. If heads, prevent all damage done to this Pokémon by attacks during your opponent\'s next turn.'
    },
    {
      name: 'Discharge',
      cost: [L],
      damage: 30,
      damageCalculation: 'x' as 'x',
      text: 'Discard all [L] Energy from this Pokémon. This attack does 30 damage for each card you discarded in this way.'
    }
  ];

  public set: string = 'SUM';
  public setNumber: string = '53';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Togedemaru';
  public fullName: string = 'Togedemaru SUM';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Defense Curl
    // Ref: set-mysterious-treasures/chansey.ts (Scrunch)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      return COIN_FLIP_PROMPT(store, state, player, result => {
        if (!result) {
          return;
        }
        ADD_MARKER(this.DEFENSE_CURL_MARKER, player.active, this);
        ADD_MARKER(this.CLEAR_DEFENSE_CURL_MARKER, opponent, this);
      });
    }

    // Attack 2: Discharge
    // Refs: AGENTS.md (mandatory discard all type energy pattern), set-primal-clash/manectric.ts
    if (WAS_ATTACK_USED(effect, 1, this)) {
      const player = effect.player;
      const cards = player.active.cards.filter(c =>
        c instanceof EnergyCard && c.provides.includes(CardType.LIGHTNING)
      );
      const discardCount = cards.length;
      cards.forEach(c => { player.active.moveCardTo(c, player.discard); });
      effect.damage = 30 * discardCount;
    }

    PREVENT_DAMAGE_IF_TARGET_HAS_MARKER(effect, this.DEFENSE_CURL_MARKER, this);
    CLEAR_MARKER_AND_OPPONENTS_POKEMON_MARKER_AT_END_OF_TURN(state, effect, this.CLEAR_DEFENSE_CURL_MARKER, this.DEFENSE_CURL_MARKER, this);

    return state;
  }
}
