// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, PlayerType, StoreLike, State, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PlayItemEffect } from '../../game/store/effects/play-card-effects';
import { IS_ABILITY_BLOCKED } from '../../game/store/prefabs/prefabs';

export class Omastar extends PokemonCard {
  public stage: Stage = Stage.STAGE_2;
  public evolvesFrom: string = 'Omanyte';
  public cardType: CardType = F;
  public hp: number = 130;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public powers = [{
    name: 'Fossil Bind',
    powerType: PowerType.ABILITY,
    text: 'As long as you have fewer Pokémon in play than your opponent, they can\'t play any Item cards from their hand.'
  }];

  public attacks = [
    {
      name: 'Bite',
      cost: [F, C],
      damage: 60,
      text: ''
    }
  ];

  public set: string = 'TEU';
  public setNumber: string = '76';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Omastar';
  public fullName: string = 'Omastar TEU';

  // Ref: set-x-and-y/trevenant.ts (Forest's Curse - PlayItemEffect block)
  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Fossil Bind (passive - block opponent's items)
    if (effect instanceof PlayItemEffect) {
      const player = effect.player;

      // Check if Omastar is on opponent's side
      let omastarOwner: any = null;
      state.players.forEach(p => {
        p.forEachPokemon(PlayerType.BOTTOM_PLAYER, (cardList) => {
          if (cardList.getPokemonCard() === this) {
            omastarOwner = p;
          }
        });
      });

      if (!omastarOwner) {
        return state;
      }

      // The opponent of Omastar's owner must be the one playing the item
      if (omastarOwner === player) {
        return state;
      }

      // Check if Omastar's owner has fewer Pokemon in play
      let ownerPokemonCount = 0;
      omastarOwner.forEachPokemon(PlayerType.BOTTOM_PLAYER, () => {
        ownerPokemonCount++;
      });

      let opponentPokemonCount = 0;
      player.forEachPokemon(PlayerType.BOTTOM_PLAYER, () => {
        opponentPokemonCount++;
      });

      if (ownerPokemonCount >= opponentPokemonCount) {
        return state;
      }

      // Check if ability is blocked
      if (IS_ABILITY_BLOCKED(store, state, omastarOwner, this)) {
        return state;
      }

      throw new GameError(GameMessage.BLOCKED_BY_ABILITY);
    }

    return state;
  }
}
