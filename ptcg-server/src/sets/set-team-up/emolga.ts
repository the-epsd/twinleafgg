// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { ABILITY_USED, ADD_PARALYZED_TO_PLAYER_ACTIVE, AFTER_ATTACK, BLOCK_IF_DECK_EMPTY, COIN_FLIP_PROMPT, IS_ABILITY_BLOCKED, REMOVE_MARKER_AT_END_OF_TURN, SHUFFLE_DECK, USE_ABILITY_ONCE_PER_TURN, WAS_POWER_USED } from '../../game/store/prefabs/prefabs';
import { CardType, Stage, SuperType } from '../../game/store/card/card-types';
import { GameError } from '../../game/game-error';
import { GameMessage } from '../../game/game-message';
import { ChooseCardsPrompt } from '../../game/store/prompts/choose-cards-prompt';
import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Effect } from '../../game/store/effects/effect';
import { Card, PowerType, State, StoreLike } from '../../game';
export class Emolga extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = L;
  public hp: number = 60;
  public weakness = [{ type: L }];
  public resistance = [{ type: F, value: -20 }];
  public retreat = [];

  public readonly NUZZLY_GATHERING_MARKER = 'NUZZLY_GATHERING_MARKER';

  public powers = [{
    name: 'Nuzzly Gathering',
    useWhenInPlay: true,
    powerType: PowerType.ABILITY,
    text: 'Once during your turn (before your attack), you may search your deck for a Pokémon that has the Nuzzle attack, reveal it, and put it into your hand. Then, shuffle your deck.'
  }];

  public attacks = [
    {
      name: 'Nuzzle',
      cost: [L],
      damage: 0,
      text: 'Flip a coin. If heads, your opponent\'s Active Pokémon is now Paralyzed.'
    }
  ];

  public set: string = 'TEU';
  public setNumber: string = '46';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Emolga';
  public fullName: string = 'Emolga TEU';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Nuzzly Gathering
    // Ref: set-lost-thunder/dedenne-2.ts (Nuzzle attack search)
    if (WAS_POWER_USED(effect, 0, this)) {
      const player = effect.player;

      if (IS_ABILITY_BLOCKED(store, state, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }

      BLOCK_IF_DECK_EMPTY(player);
      USE_ABILITY_ONCE_PER_TURN(player, this.NUZZLY_GATHERING_MARKER, this);
      ABILITY_USED(player, this);

      // Block Pokemon that don't have the Nuzzle attack
      const blocked: number[] = [];
      player.deck.cards.forEach((c, index) => {
        if (!(c instanceof PokemonCard) || !c.attacks.some(a => a.name === 'Nuzzle')) {
          blocked.push(index);
        }
      });

      return store.prompt(state, new ChooseCardsPrompt(
        player,
        GameMessage.CHOOSE_CARD_TO_HAND,
        player.deck,
        { superType: SuperType.POKEMON },
        { min: 0, max: 1, allowCancel: true, blocked }
      ), selected => {
        const cards: Card[] = selected || [];
        cards.forEach(c => { player.deck.moveCardTo(c, player.hand); });
        return SHUFFLE_DECK(store, state, player);
      });
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.NUZZLY_GATHERING_MARKER, this);

    // Attack 1: Nuzzle
    // Ref: set-lost-thunder/dedenne.ts (Nuzzle)
    if (AFTER_ATTACK(effect, 0, this)) {
      COIN_FLIP_PROMPT(store, state, effect.player, result => {
        if (result) {
          ADD_PARALYZED_TO_PLAYER_ACTIVE(store, state, effect.opponent, this);
        }
      });
    }

    return state;
  }
}
