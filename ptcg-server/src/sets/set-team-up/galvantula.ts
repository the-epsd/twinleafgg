// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { PowerType, PlayerType, StoreLike, State, StateUtils } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { TrainerTargetEffect } from '../../game/store/effects/play-card-effects';
import { WAS_ATTACK_USED, IS_ABILITY_BLOCKED, SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND } from '../../game/store/prefabs/prefabs';

export class Galvantula extends PokemonCard {
  public stage: Stage = Stage.STAGE_1;
  public evolvesFrom: string = 'Joltik';
  public cardType: CardType = L;
  public hp: number = 90;
  public weakness = [{ type: F }];
  public resistance = [{ type: M, value: -20 }];
  public retreat = [C];

  public powers = [{
    name: 'Unnerve',
    powerType: PowerType.ABILITY,
    text: 'Whenever your opponent plays an Item or Supporter card from their hand, prevent all effects of that card done to this Pokémon.'
  }];

  public attacks = [
    {
      name: 'Spider Thread',
      cost: [L],
      damage: 40,
      text: 'Put a card from your discard pile into your hand.'
    }
  ];

  public set: string = 'TEU';
  public setNumber: string = '48';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Galvantula';
  public fullName: string = 'Galvantula TEU';

  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Ability: Unnerve (passive - prevent trainer effects targeting this Pokemon)
    // Ref: set-forbidden-light/pyroar.ts (Unnerve - TrainerTargetEffect pattern)
    if (effect instanceof TrainerTargetEffect && effect.target?.cards.includes(this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      if (IS_ABILITY_BLOCKED(store, state, opponent, this)) {
        return state;
      }

      let isGalvantulaOnOpponentsSide = false;
      opponent.forEachPokemon(PlayerType.BOTTOM_PLAYER, cardList => {
        if (cardList.getPokemonCard() === this) {
          isGalvantulaOnOpponentsSide = true;
        }
      });
      if (!isGalvantulaOnOpponentsSide) {
        return state;
      }

      effect.target = undefined;
    }

    // Attack 1: Spider Thread
    // Ref: set-lost-thunder/houndoom.ts (SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND)
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      SEARCH_DISCARD_PILE_FOR_CARDS_TO_HAND(store, state, player, this,
        {},
        { min: 1, max: 1, allowCancel: false }
      );
    }

    return state;
  }
}
