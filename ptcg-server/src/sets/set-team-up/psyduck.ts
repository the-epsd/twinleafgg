// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.

import { PokemonCard } from '../../game/store/card/pokemon-card';
import { Stage, CardType } from '../../game/store/card/card-types';
import { StoreLike, State, StateUtils, GameError, GameMessage } from '../../game';
import { Effect } from '../../game/store/effects/effect';
import { PlayItemEffect, PlaySupporterEffect, PlayStadiumEffect } from '../../game/store/effects/play-card-effects';
import { WAS_ATTACK_USED, ADD_MARKER, HAS_MARKER, REMOVE_MARKER_AT_END_OF_TURN, COIN_FLIP_PROMPT } from '../../game/store/prefabs/prefabs';

export class Psyduck extends PokemonCard {
  public stage: Stage = Stage.BASIC;
  public cardType: CardType = W;
  public hp: number = 60;
  public weakness = [{ type: G }];
  public retreat = [C, C];

  public readonly HEADACHE_MARKER = 'PSYDUCK_TEU_HEADACHE_MARKER';

  public attacks = [
    {
      name: 'Headache',
      cost: [C],
      damage: 10,
      text: 'Flip a coin. If heads, your opponent can\'t play any Trainer cards from their hand during their next turn.'
    }
  ];

  public set: string = 'TEU';
  public setNumber: string = '26';
  public cardImage: string = 'assets/cardback.png';
  public name: string = 'Psyduck';
  public fullName: string = 'Psyduck TEU';

  // Refs: set-unified-minds/beheeyem.ts (Item block with markers), set-x-and-y/trevenant.ts (trainer block)
  public reduceEffect(store: StoreLike, state: State, effect: Effect): State {
    // Attack 1: Headache
    if (WAS_ATTACK_USED(effect, 0, this)) {
      const player = effect.player;
      const opponent = StateUtils.getOpponent(state, player);

      COIN_FLIP_PROMPT(store, state, player, result => {
        if (result) {
          ADD_MARKER(this.HEADACHE_MARKER, opponent, this);
        }
      });
    }

    // Block all Trainer cards (Items, Supporters, Stadiums)
    if (effect instanceof PlayItemEffect) {
      const player = effect.player;
      if (HAS_MARKER(this.HEADACHE_MARKER, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }
    }

    if (effect instanceof PlaySupporterEffect) {
      const player = effect.player;
      if (HAS_MARKER(this.HEADACHE_MARKER, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }
    }

    if (effect instanceof PlayStadiumEffect) {
      const player = effect.player;
      if (HAS_MARKER(this.HEADACHE_MARKER, player, this)) {
        throw new GameError(GameMessage.BLOCKED_BY_EFFECT);
      }
    }

    REMOVE_MARKER_AT_END_OF_TURN(effect, this.HEADACHE_MARKER, this);

    return state;
  }
}
