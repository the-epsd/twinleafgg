/**
 * Username whitelist for 3D board access.
 * Add usernames to this array to grant 3D board access.
 * Usernames are case-sensitive and must match exactly.
 * Server restart required for changes to take effect.
 */
export const board3dWhitelist: string[] = [
  // Add usernames here, e.g.:
  // 'username1',
  // 'username2',
  'TheEPSD'
];
