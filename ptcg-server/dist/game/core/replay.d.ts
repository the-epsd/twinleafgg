import { State, GameWinner } from '../store/state/state';
import { ReplayPlayer, ReplayOptions, ReplayActionRecord } from './replay.interface';
export declare class Replay {
    private readonly indexJumpSize;
    player1: ReplayPlayer;
    player2: ReplayPlayer;
    winner: GameWinner;
    created: number;
    private gameSettings;
    private turnMap;
    private diffs;
    private indexes;
    private actions;
    private prevState;
    private serializer;
    private options;
    constructor(options?: Partial<ReplayOptions>);
    getStateCount(): number;
    getActions(): ReplayActionRecord[];
    getGameSettings(): any | undefined;
    getState(position: number): State;
    getTurnCount(): number;
    getTurnPosition(turn: number): number;
    setCreated(created: number): void;
    setGameSettings(gameSettings: any): void;
    appendState(state: State): void;
    appendAction(type: string, payload: any, state: Pick<State, 'turn' | 'phase' | 'activePlayer'>, stateIndex: number): void;
    serialize(): string;
    deserialize(replayData: string): void;
    private swapQuotes;
    private compress;
    private decompress;
    private sanitizeReplayValue;
    private cloneReplayValue;
    private isCardLike;
    private copyIfPresent;
    private rebuildIndex;
    private indexJumps;
}
